{
  "name": "Booking.com dated hotel shortlist -> Google Sheets (Apify)",
  "nodes": [
    {
      "parameters": {
        "content": "## Booking.com hotel shortlist -> Google Sheets\n\nThis workflow runs the locked Luminar Booking.com Hotel Scraper once and upserts a dated, filterable property shortlist into Google Sheets. Every row stays tied to one destination, check-in, check-out, and requested currency context; it does not present search-card prices as checkout-verified quotes.\n\n### How it works\n\n1. A manual test or inactive daily schedule reaches one editable settings node.\n2. Fail-closed preflight validates all placeholders, destination, dates, guests, enums, result/request/runtime limits, spreadsheet target, and maximum charge before paid work.\n3. The exact Actor build runs once with automatic paid retry disabled.\n4. n8n reads the Actor's documented OUTPUT summary, normalizes coverage and PPE accounting, and retrieves every intended property and coverage row through bounded pagination.\n5. Stable Google Sheets keys use the locale-neutral canonical Booking property path + checkIn + checkOut + currency. Numeric-ID and path-fallback source routes therefore update the same row.\n\n### Setup\n\n- [ ] Install the verified Apify community node.\n- [ ] Select the same Apify credential on both Apify nodes. The signed summary request needs no credential.\n- [ ] Select a Google Sheets OAuth credential on the final destination node.\n- [ ] Replace both date placeholders and the spreadsheet ID; confirm the sheet name.\n- [ ] Before any paid run, copy row 1 exactly from workflows/sheets/booking-hotels-shortlist-header.csv into the target tab, then refresh the Google Sheets node columns.\n- [ ] Run manually and inspect coverage, output rows, exact charge, and the materialized Sheet rows.\n- [ ] Activate the schedule only after the bounded test is correct.\n\nVerified empty output creates no Sheet row. Partial or failed coverage also creates no row. An intentional bounded cap may write useful rows, but every row and final receipt discloses the capped state.",
        "width": 720,
        "height": 960
      },
      "id": "798189eb779b2691d9464686",
      "name": "Creator overview",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -880,
        -360
      ]
    },
    {
      "parameters": {
        "content": "## 1. Configure safely\n\nStart manually or on an inactive schedule. One settings node and fail-closed preflight protect paid work and the Sheet destination.",
        "width": 880,
        "height": 760,
        "color": 7
      },
      "id": "e47451219da4da8cf09f1d1f",
      "name": "Section 1 - Configure safely",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -96,
        -360
      ]
    },
    {
      "parameters": {
        "content": "## 2. Run once and read evidence\n\nRuns the immutable Actor once without paid retry, classifies terminal status, and reads the documented OUTPUT summary.",
        "width": 960,
        "height": 760,
        "color": 7
      },
      "id": "9d0d6603434d971b06075e6f",
      "name": "Section 2 - Run once and read evidence",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        832,
        -360
      ]
    },
    {
      "parameters": {
        "content": "## 3. Retrieve complete output\n\nNormalizes coverage and exact PPE accounting, plans bounded pages, and retrieves all property plus coverage rows.",
        "width": 1120,
        "height": 760,
        "color": 7
      },
      "id": "73577385521dc0d048af1445",
      "name": "Section 3 - Retrieve complete output",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        1824,
        -360
      ]
    },
    {
      "parameters": {
        "content": "## 4. Build stable upserts\n\nReconciles counts and coverage, then creates route-independent keys from the normalized Booking property path, stay dates, and currency.",
        "width": 1000,
        "height": 760,
        "color": 7
      },
      "id": "3dc3594f4104b56c3085fce1",
      "name": "Section 4 - Build stable upserts",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        2976,
        -360
      ]
    },
    {
      "parameters": {
        "content": "## 5. Write or stop honestly\n\nRetries only Google Sheets delivery. Verified empty, partial, failed, and setup errors return explicit no-write receipts.",
        "width": 800,
        "height": 760,
        "color": 7
      },
      "id": "0fc6b0cfbfbe9345d654d644",
      "name": "Section 5 - Write or stop honestly",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        4008,
        -360
      ]
    },
    {
      "parameters": {},
      "id": "ee96019f0f4903baa52cbf8a",
      "name": "Test manually",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        0,
        200
      ]
    },
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "days",
              "daysInterval": 1,
              "triggerAtHour": 8,
              "triggerAtMinute": 0
            }
          ]
        }
      },
      "id": "a28d68663e1251c5989acb58",
      "name": "Every day at 08:00 (inactive)",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        0,
        0
      ],
      "disabled": true
    },
    {
      "parameters": {
        "assignments": {
          "assignments": [
            {
              "id": "065f20ef38e7172d4043be13",
              "name": "destination",
              "value": "Tivat, Montenegro",
              "type": "string"
            },
            {
              "id": "c7923dc328227673c3309a6f",
              "name": "checkIn",
              "value": "REPLACE_WITH_CHECK_IN",
              "type": "string"
            },
            {
              "id": "ce0e9192e06176977c3ac01a",
              "name": "checkOut",
              "value": "REPLACE_WITH_CHECK_OUT",
              "type": "string"
            },
            {
              "id": "ee711d5953afbe2340826032",
              "name": "adults",
              "value": 2,
              "type": "number"
            },
            {
              "id": "fa70396ed9fd2fe58294de4a",
              "name": "rooms",
              "value": 1,
              "type": "number"
            },
            {
              "id": "95d9c0fae0bf433acb615573",
              "name": "maxProperties",
              "value": 20,
              "type": "number"
            },
            {
              "id": "57d3e6e0c90896e53a703e32",
              "name": "maxPagesPerTarget",
              "value": 1,
              "type": "number"
            },
            {
              "id": "271a768ebddf8f14dcce6ac5",
              "name": "maxRequests",
              "value": 5,
              "type": "number"
            },
            {
              "id": "9e7449e21dd1db9ba4703b4c",
              "name": "maxRuntimeSecs",
              "value": 300,
              "type": "number"
            },
            {
              "id": "74d573bb5108a4503212a072",
              "name": "sortBy",
              "value": "popularity",
              "type": "string"
            },
            {
              "id": "2013895d4d81c5fe3e48628e",
              "name": "currency",
              "value": "EUR",
              "type": "string"
            },
            {
              "id": "e7e24cad5add53f8177ae98b",
              "name": "language",
              "value": "en-gb",
              "type": "string"
            },
            {
              "id": "df8141d109fa04285b536683",
              "name": "spreadsheetId",
              "value": "REPLACE_WITH_SPREADSHEET_ID",
              "type": "string"
            },
            {
              "id": "144c1dbc1081283e752dbff2",
              "name": "sheetName",
              "value": "Booking Shortlist",
              "type": "string"
            },
            {
              "id": "0422137808636953df16b3a8",
              "name": "maxCostPerRunUsd",
              "value": 0.1,
              "type": "number"
            }
          ]
        },
        "options": {}
      },
      "id": "075e88ff756fc9d054cb27a0",
      "name": "Shortlist settings",
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.4,
      "position": [
        240,
        100
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// runtimeSourceSha256=a1f7a6f4e65bed3b976cad220ac0fc356239532e6427be18a40d9e7eb1f3ea5c\nconst CURRENCIES = new Set(['EUR', 'USD', 'GBP', 'CHF', 'CAD', 'AUD', 'JPY', 'PLN', 'AED', 'INR', 'TRY']);\nconst LANGUAGES = new Set(['en-gb', 'en-us', 'de', 'fr', 'es', 'it', 'nl', 'pt-pt', 'pl', 'ja', 'ko', 'zh-cn']);\nconst SORTS = new Set(['popularity', 'price_low', 'price_high', 'stars', 'distance', 'review_score']);\nconst MAX_PROPERTY_ROWS = 1000;\nconst MAX_DATASET_ROWS = 1001;\nconst PAGE_SIZE = 1000;\nconst DATE_RE = /^\\d{4}-\\d{2}-\\d{2}$/;\n\nfunction fail(code) {\n  throw new Error(code);\n}\n\nfunction upper(value) {\n  return String(value ?? '').trim().toUpperCase();\n}\n\nfunction boundedInteger(value, code, minimum, maximum) {\n  const parsed = Number(value);\n  if (!Number.isInteger(parsed) || parsed < minimum || parsed > maximum) fail(code);\n  return parsed;\n}\n\nfunction positiveFinite(value, code, minimum, maximum) {\n  const parsed = Number(value);\n  if (!Number.isFinite(parsed) || parsed < minimum || parsed > maximum) fail(code);\n  return parsed;\n}\n\nfunction isoDate(value, placeholderCode, invalidCode) {\n  const text = String(value ?? '').trim();\n  if (!text || text.startsWith('REPLACE_WITH_')) fail(placeholderCode);\n  if (!DATE_RE.test(text) || Number.isNaN(Date.parse(`${text}T00:00:00Z`))) fail(invalidCode);\n  return text;\n}\n\nfunction preflightBookingHotelsConfig(raw = {}) {\n  const destination = String(raw.destination ?? '').trim();\n  if (!destination || destination === 'REPLACE_WITH_DESTINATION' || destination.length > 160) fail('DESTINATION_NOT_CONFIGURED');\n  const checkIn = isoDate(raw.checkIn, 'CHECK_IN_NOT_CONFIGURED', 'INVALID_CHECK_IN_DATE');\n  const checkOut = isoDate(raw.checkOut, 'CHECK_OUT_NOT_CONFIGURED', 'INVALID_CHECK_OUT_DATE');\n  if (Date.parse(`${checkOut}T00:00:00Z`) <= Date.parse(`${checkIn}T00:00:00Z`)) fail('CHECK_OUT_MUST_BE_AFTER_CHECK_IN');\n\n  const spreadsheetId = String(raw.spreadsheetId ?? '').trim();\n  const sheetName = String(raw.sheetName ?? '').trim();\n  if (!spreadsheetId || spreadsheetId === 'REPLACE_WITH_SPREADSHEET_ID') fail('SPREADSHEET_ID_NOT_CONFIGURED');\n  if (!/^[a-zA-Z0-9-_]{20,}$/.test(spreadsheetId)) fail('INVALID_SPREADSHEET_ID');\n  if (!sheetName || sheetName === 'REPLACE_WITH_SHEET_NAME' || sheetName.length > 100) fail('SHEET_NAME_NOT_CONFIGURED');\n\n  const adults = boundedInteger(raw.adults, 'INVALID_ADULTS_1_TO_30', 1, 30);\n  const rooms = boundedInteger(raw.rooms, 'INVALID_ROOMS_1_TO_30', 1, 30);\n  const maxProperties = boundedInteger(raw.maxProperties, 'INVALID_MAX_PROPERTIES_1_TO_1000', 1, MAX_PROPERTY_ROWS);\n  const maxPagesPerTarget = boundedInteger(raw.maxPagesPerTarget, 'INVALID_MAX_PAGES_1_TO_40', 1, 40);\n  const maxRequests = boundedInteger(raw.maxRequests, 'INVALID_MAX_REQUESTS_1_TO_250', 1, 250);\n  const maxRuntimeSecs = boundedInteger(raw.maxRuntimeSecs, 'INVALID_MAX_RUNTIME_30_TO_3600', 30, 3600);\n\n  const currency = upper(raw.currency || 'EUR');\n  const language = String(raw.language ?? 'en-gb').trim().toLowerCase();\n  const sortBy = String(raw.sortBy ?? 'popularity').trim();\n  if (!CURRENCIES.has(currency)) fail('INVALID_CURRENCY');\n  if (!LANGUAGES.has(language)) fail('INVALID_LANGUAGE');\n  if (!SORTS.has(sortBy)) fail('INVALID_SORT_ORDER');\n  const maxCostPerRunUsd = positiveFinite(raw.maxCostPerRunUsd, 'INVALID_MAX_CHARGE_CAP', 0.02139, 5);\n\n  return {\n    destination,\n    checkIn,\n    checkOut,\n    currency,\n    spreadsheetId,\n    sheetName,\n    targetCount: 1,\n    maxProperties,\n    maxCostPerRunUsd,\n    actorInput: {\n      destination,\n      checkIn,\n      checkOut,\n      adults,\n      rooms,\n      children: 0,\n      childrenAges: [],\n      flexibleDays: '0',\n      maxProperties,\n      mode: 'fast',\n      sortBy,\n      currency,\n      language,\n      maxPagesPerTarget,\n      maxRequests,\n      maxRuntimeSecs,\n      maxCostUsd: 0.25,\n      maxBuyerChargeUsd: maxCostPerRunUsd,\n      requestTimeoutSecs: 35,\n      maxRetries: 1,\n    },\n  };\n}\n\nfunction classifyBookingTerminalRun(run = {}, expectedBuildId = '') {\n  const status = upper(run.status);\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  if (run.error || !status) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'APIFY_RUN_NODE_ERROR' };\n  }\n  if (status !== 'SUCCEEDED') {\n    return { ok: false, state: 'ACTOR_FAILED_OR_ABORTED', run, runId, runUrl, reason: `ACTOR_STATUS_${status}` };\n  }\n  if (!expectedBuildId || String(run.buildId ?? '') !== String(expectedBuildId)) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'RUN_BUILD_ID_MISMATCH' };\n  }\n  if (!run.defaultKeyValueStoreId || !run.defaultDatasetId) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'RUN_STORAGE_IDS_MISSING' };\n  }\n  return { ok: true, state: 'RUN_SUCCEEDED', run, runId, runUrl };\n}\n\nfunction exactBookingBuyerCharge(billing) {\n  if (billing?.billingMode !== 'pay_per_event') fail('PPE_PRICING_NOT_ACTIVE');\n  const prices = billing.effectiveEventPricesUsd;\n  const counts = billing.counts;\n  if (!prices || typeof prices !== 'object' || !counts || typeof counts !== 'object') {\n    fail('AUTHORITATIVE_PPE_ACCOUNTING_MISSING');\n  }\n  const chargedCounts = {};\n  let total = 0;\n  for (const [eventName, eventCounts] of Object.entries(counts)) {\n    const charged = Number(eventCounts?.charged ?? 0);\n    if (!Number.isInteger(charged) || charged < 0) fail('INVALID_CHARGED_EVENT_COUNT');\n    chargedCounts[eventName] = charged;\n    if (charged === 0) continue;\n    const price = Number(prices[eventName]);\n    if (!Number.isFinite(price) || price < 0) fail('INVALID_EFFECTIVE_EVENT_PRICE');\n    total += charged * price;\n  }\n  return {\n    actualBuyerChargeUsd: Number(total.toFixed(12)),\n    chargedCounts,\n    effectivePricesUsd: { ...prices },\n  };\n}\n\nfunction parseBookingOutputRecord(record = {}, run = {}) {\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  const identity = { run, runId, runUrl };\n  const recordIsObject = record && typeof record === 'object' && !Array.isArray(record);\n  const data = recordIsObject && !record.error\n    ? (Object.prototype.hasOwnProperty.call(record, 'data') ? record.data : record)\n    : null;\n  if (!data || typeof data !== 'object' || Array.isArray(data) || data.error) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_SUMMARY_MISSING_OR_INVALID' };\n  }\n  let ppe;\n  try { ppe = exactBookingBuyerCharge(data.billing); } catch (error) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: error.message };\n  }\n  const outputRows = Number(data.propertyRows);\n  const targetCount = Number(data.targetCount);\n  if (!Number.isInteger(outputRows) || outputRows < 0 || outputRows > MAX_PROPERTY_ROWS) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_PROPERTY_ROW_COUNT_INVALID' };\n  }\n  const zeroCapacityGuard = targetCount === 0\n    && outputRows === 0\n    && upper(data.coverageState) === 'CAPPED'\n    && upper(data.coverageStatus) === 'CAPPED'\n    && data.stoppedByBillingGuard === true\n    && Array.isArray(data.perTarget)\n    && data.perTarget.length === 0;\n  if (!Number.isInteger(targetCount) || targetCount < 0 || !Array.isArray(data.perTarget)\n    || data.perTarget.length !== targetCount || (targetCount === 0 && !zeroCapacityGuard)) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_TARGET_CONTRACT_MISSING' };\n  }\n  if (Number(ppe.chargedCounts.fast_property_result ?? 0) !== outputRows) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'PPE_PROPERTY_DELIVERY_COUNT_MISMATCH' };\n  }\n  return {\n    ok: true,\n    state: 'SUMMARY_READY',\n    ...identity,\n    buyerChargeUsd: ppe.actualBuyerChargeUsd,\n    summary: {\n      schemaVersion: 2,\n      sourceRecordKey: 'OUTPUT',\n      status: upper(data.status),\n      coverageState: upper(data.coverageState),\n      coverageStatus: upper(data.coverageStatus),\n      outputRows,\n      expectedDatasetRows: outputRows + 1,\n      targetCount,\n      perTarget: data.perTarget,\n      warnings: Array.isArray(data.warnings) ? data.warnings.map(String) : [],\n      ppe: {\n        ...ppe,\n        deliveryEventLimitReached: data.billing?.eventLimitReached === true || data.stoppedByBillingGuard === true,\n      },\n    },\n  };\n}\n\nfunction planBookingDatasetPages(summary, pageSize = PAGE_SIZE) {\n  const total = boundedInteger(summary?.expectedDatasetRows, 'INVALID_SUMMARY_DATASET_ROWS', 1, MAX_DATASET_ROWS);\n  const size = boundedInteger(pageSize, 'INVALID_DATASET_PAGE_SIZE', 1, PAGE_SIZE);\n  return Array.from({ length: Math.ceil(total / size) }, (_, index) => ({\n    offset: index * size,\n    limit: Math.min(size, total - (index * size)),\n    expectedRows: total,\n  }));\n}\n\nfunction normalizedBookingPropertyPath(canonicalUrl) {\n  try {\n    const url = new URL(String(canonicalUrl ?? '').trim());\n    if (!/(^|\\.)booking\\.com$/i.test(url.hostname) || !/^\\/hotel\\//i.test(url.pathname)) return null;\n    return url.pathname.toLowerCase()\n      .replace(/\\/{2,}/g, '/')\n      .replace(/\\.[a-z]{2}(?:-[a-z]{2})?\\.html$/i, '.html');\n  } catch {\n    return null;\n  }\n}\n\nfunction bookingStableIdentity({ stablePropertyKey, canonicalUrl, propertyId }) {\n  const canonicalPath = normalizedBookingPropertyPath(canonicalUrl);\n  if (canonicalPath) return `booking_path:${canonicalPath}`;\n  const actorStableKey = String(stablePropertyKey ?? '').trim();\n  if (actorStableKey) return actorStableKey;\n  const id = String(propertyId ?? '').trim();\n  if (!id) fail('PROPERTY_ID_REQUIRED_FOR_STABLE_UPSERT');\n  return id;\n}\n\nfunction bookingUpsertKey({ stablePropertyKey, canonicalUrl, propertyId, checkIn, checkOut, currency }) {\n  const identity = bookingStableIdentity({ stablePropertyKey, canonicalUrl, propertyId });\n  if (!DATE_RE.test(String(checkIn ?? '')) || !DATE_RE.test(String(checkOut ?? ''))) fail('DATES_REQUIRED_FOR_STABLE_UPSERT');\n  const normalizedCurrency = upper(currency);\n  if (!CURRENCIES.has(normalizedCurrency)) fail('CURRENCY_REQUIRED_FOR_STABLE_UPSERT');\n  return `${identity}|${checkIn}|${checkOut}|${normalizedCurrency}`;\n}\n\nfunction scalar(value) {\n  if (value === null || value === undefined) return '';\n  if (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean') return value;\n  return JSON.stringify(value);\n}\n\nfunction prepareSheetRow(row, config, context) {\n  const search = row.searchParameters;\n  if (!search || typeof search !== 'object'\n    || String(search.checkIn ?? '') !== config.checkIn\n    || String(search.checkOut ?? '') !== config.checkOut\n    || upper(search.currency) !== config.currency) {\n    fail('PROPERTY_SEARCH_CONTEXT_MISMATCH');\n  }\n  const upsertKey = bookingUpsertKey({\n    stablePropertyKey: row.stablePropertyKey,\n    canonicalUrl: row.canonicalUrl,\n    propertyId: row.propertyId,\n    checkIn: config.checkIn,\n    checkOut: config.checkOut,\n    currency: config.currency,\n  });\n  return {\n    upsertKey,\n    stablePropertyKey: scalar(row.stablePropertyKey || bookingStableIdentity(row)),\n    propertyId: scalar(row.propertyId),\n    name: scalar(row.name),\n    checkIn: config.checkIn,\n    checkOut: config.checkOut,\n    requestedCurrency: config.currency,\n    resultCurrency: scalar(row.priceCurrency),\n    stayTotalAmount: scalar(row.stayTotalAmount),\n    stayNights: scalar(row.stayNights),\n    nightlyAmount: scalar(row.nightlyAmount),\n    guestReviewScore: scalar(row.guestReviewScore),\n    guestReviewCount: scalar(row.guestReviewCount),\n    stars: scalar(row.stars),\n    availabilityStatus: scalar(row.availabilityStatus),\n    locationText: scalar(row.locationText),\n    canonicalUrl: scalar(row.canonicalUrl),\n    priceBasis: scalar(row.priceBasis),\n    checkoutVerified: scalar(row.checkoutVerified),\n    collectedAt: scalar(row.collectedAt),\n    coverageState: context.coverageState,\n    workflowState: context.workflowState,\n    exactActorChargeUsd: context.buyerChargeUsd,\n    actorRunId: context.runId,\n  };\n}\n\nfunction reconcileBookingHotelsRun({ run = {}, summary = {}, rows = [], config = {} }) {\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  const datasetUrl = run.defaultDatasetId\n    ? `https://console.apify.com/storage/datasets/${run.defaultDatasetId}`\n    : runUrl;\n  const buyerChargeUsd = Number(summary?.ppe?.actualBuyerChargeUsd);\n  const base = {\n    run, summary, runId, runUrl, datasetUrl, buyerChargeUsd,\n    baselineState: 'NOT_APPLICABLE_STATELESS_SNAPSHOT',\n    unchangedState: 'NOT_CLAIMED_GOOGLE_SHEETS_UPSERT_ONLY',\n  };\n  if (!Number.isFinite(buyerChargeUsd) || buyerChargeUsd < 0) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'AUTHORITATIVE_BUYER_CHARGE_MISSING', shouldWrite: false, sheetRows: [] };\n  }\n  const zeroCapacityGuard = Number(summary.targetCount) === 0\n    && Number(summary.outputRows) === 0\n    && upper(summary.coverageState) === 'CAPPED'\n    && summary.ppe?.deliveryEventLimitReached === true;\n  if (!zeroCapacityGuard && Number(summary.targetCount) !== Number(config.targetCount)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'TARGET_RECONCILIATION_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n  if (rows.some((row) => row?.error)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DATASET_READ_ERROR', shouldWrite: false, sheetRows: [] };\n  }\n  const normalized = rows.filter((row) => row && typeof row === 'object' && Object.keys(row).length && !row.error);\n  const properties = normalized.filter((row) => row.recordType === 'property');\n  const coverageRows = normalized.filter((row) => row.recordType === 'coverage');\n  if (coverageRows.length !== 1 || normalized.length !== Number(summary.expectedDatasetRows)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DATASET_OR_COVERAGE_COUNT_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n  if (properties.length !== Number(summary.outputRows)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'PROPERTY_ROW_COUNT_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n\n  const coverageRecord = coverageRows[0];\n  if (Number(coverageRecord.targetCount) !== Number(summary.targetCount)\n    || upper(coverageRecord.coverageState) !== upper(summary.coverageState)\n    || upper(coverageRecord.coverageStatus) !== upper(summary.coverageStatus)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'OUTPUT_DATASET_COVERAGE_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n\n  if (zeroCapacityGuard) {\n    return {\n      ...base,\n      state: 'CAPPED',\n      reason: 'PPE_ALLOWANCE_ZERO_SOURCE_NOT_STARTED',\n      coverageState: 'CAPPED',\n      shouldWrite: false,\n      sheetRows: [],\n    };\n  }\n\n  const coverageState = upper(summary.coverageState || coverageRecord.coverageState);\n  const status = upper(summary.status || summary.coverageStatus);\n  if (['FAILED', 'BLOCKED'].includes(coverageState) || status === 'FAILED') {\n    return { ...base, state: 'FAILED', reason: 'SOURCE_FAILED_OR_BLOCKED', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (coverageState === 'PARTIAL' || status === 'PARTIAL') {\n    return { ...base, state: 'PARTIAL', reason: 'SOURCE_COVERAGE_PARTIAL', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  const workflowState = coverageState === 'CAPPED' || summary.ppe?.deliveryEventLimitReached === true\n    ? 'CAPPED'\n    : 'READY_TO_WRITE';\n\n  const seen = new Set();\n  const sheetRows = [];\n  try {\n    for (const property of properties) {\n      const row = prepareSheetRow(property, config, { coverageState, workflowState, buyerChargeUsd, runId });\n      if (seen.has(row.upsertKey)) continue;\n      seen.add(row.upsertKey);\n      sheetRows.push(row);\n    }\n  } catch (error) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: error.message, coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (sheetRows.length !== properties.length) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DUPLICATE_UPSERT_KEYS_IN_DATASET', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  const allEmpty = coverageState === 'EMPTY_CONFIRMED';\n  if (!sheetRows.length && allEmpty) {\n    return { ...base, state: 'EMPTY_VERIFIED', reason: 'SOURCE_VERIFIED_EMPTY', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (!sheetRows.length) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'ZERO_ROWS_WITHOUT_VERIFIED_EMPTY', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  return {\n    ...base,\n    state: workflowState,\n    reason: workflowState === 'CAPPED' ? 'BOUNDED_SHORTLIST_WRITTEN_WITH_CAP_DISCLOSED' : 'VERIFIED_SHORTLIST_READY',\n    coverageState,\n    shouldWrite: true,\n    sheetRows,\n  };\n}\n\nfunction renderBookingReceipt(result = {}, writtenRows = null) {\n  const labels = {\n    READY_TO_WRITE: 'Booking.com shortlist verified',\n    CAPPED: 'Booking.com bounded shortlist verified',\n    EMPTY_VERIFIED: 'Booking.com search verified empty',\n    PARTIAL: 'Booking.com search partial — no Sheet write',\n    FAILED: 'Booking.com source failed — no Sheet write',\n    ACTOR_FAILED_OR_ABORTED: 'Booking Hotels Actor failed or stopped',\n    WORKFLOW_NODE_FAILED: 'Workflow could not verify Booking.com output',\n  };\n  return {\n    state: result.state,\n    label: labels[result.state] ?? 'Booking.com shortlist result',\n    reason: result.reason ?? null,\n    rowsPrepared: Array.isArray(result.sheetRows) ? result.sheetRows.length : 0,\n    rowsWritten: Number.isInteger(writtenRows) ? writtenRows : null,\n    exactActorChargeUsd: Number.isFinite(Number(result.buyerChargeUsd)) ? Number(result.buyerChargeUsd) : null,\n    coverageState: result.coverageState ?? null,\n    baselineState: result.baselineState ?? 'NOT_APPLICABLE_STATELESS_SNAPSHOT',\n    unchangedState: result.unchangedState ?? 'NOT_CLAIMED_GOOGLE_SHEETS_UPSERT_ONLY',\n    runId: result.runId ?? null,\n    runUrl: result.runUrl ?? 'https://console.apify.com/actors/runs',\n    datasetUrl: result.datasetUrl ?? result.runUrl ?? 'https://console.apify.com/actors/runs',\n  };\n}\n\nconst BOOKING_HOTELS_WORKFLOW_LIMITS = Object.freeze({ maxPropertyRows: MAX_PROPERTY_ROWS, maxDatasetRows: MAX_DATASET_ROWS, pageSize: PAGE_SIZE });\n\n\nreturn [{ json: preflightBookingHotelsConfig($input.first().json) }];"
      },
      "id": "bb123290a79b8311e4a8e48a",
      "name": "Fail-closed preflight",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        520,
        100
      ]
    },
    {
      "parameters": {
        "authentication": "apifyOAuth2Api",
        "resource": "Actors",
        "operation": "Run actor",
        "actorSource": "recentlyUsed",
        "actorId": "luminar~booking-hotels-scraper-private-v1",
        "customBody": "={{ JSON.stringify($('Fail-closed preflight').first().json.actorInput) }}",
        "waitForFinish": true,
        "timeout": 900,
        "memory": 1024,
        "maxTotalChargeUsd": "={{ $('Fail-closed preflight').first().json.maxCostPerRunUsd }}",
        "build": "bkh-seo-260826a"
      },
      "id": "64219e99dfcaa86d97f3d387",
      "name": "Run exact Booking Hotels Actor once",
      "type": "@apify/n8n-nodes-apify.apify",
      "typeVersion": 1,
      "position": [
        896,
        100
      ],
      "retryOnFail": false,
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// runtimeSourceSha256=a1f7a6f4e65bed3b976cad220ac0fc356239532e6427be18a40d9e7eb1f3ea5c\nconst CURRENCIES = new Set(['EUR', 'USD', 'GBP', 'CHF', 'CAD', 'AUD', 'JPY', 'PLN', 'AED', 'INR', 'TRY']);\nconst LANGUAGES = new Set(['en-gb', 'en-us', 'de', 'fr', 'es', 'it', 'nl', 'pt-pt', 'pl', 'ja', 'ko', 'zh-cn']);\nconst SORTS = new Set(['popularity', 'price_low', 'price_high', 'stars', 'distance', 'review_score']);\nconst MAX_PROPERTY_ROWS = 1000;\nconst MAX_DATASET_ROWS = 1001;\nconst PAGE_SIZE = 1000;\nconst DATE_RE = /^\\d{4}-\\d{2}-\\d{2}$/;\n\nfunction fail(code) {\n  throw new Error(code);\n}\n\nfunction upper(value) {\n  return String(value ?? '').trim().toUpperCase();\n}\n\nfunction boundedInteger(value, code, minimum, maximum) {\n  const parsed = Number(value);\n  if (!Number.isInteger(parsed) || parsed < minimum || parsed > maximum) fail(code);\n  return parsed;\n}\n\nfunction positiveFinite(value, code, minimum, maximum) {\n  const parsed = Number(value);\n  if (!Number.isFinite(parsed) || parsed < minimum || parsed > maximum) fail(code);\n  return parsed;\n}\n\nfunction isoDate(value, placeholderCode, invalidCode) {\n  const text = String(value ?? '').trim();\n  if (!text || text.startsWith('REPLACE_WITH_')) fail(placeholderCode);\n  if (!DATE_RE.test(text) || Number.isNaN(Date.parse(`${text}T00:00:00Z`))) fail(invalidCode);\n  return text;\n}\n\nfunction preflightBookingHotelsConfig(raw = {}) {\n  const destination = String(raw.destination ?? '').trim();\n  if (!destination || destination === 'REPLACE_WITH_DESTINATION' || destination.length > 160) fail('DESTINATION_NOT_CONFIGURED');\n  const checkIn = isoDate(raw.checkIn, 'CHECK_IN_NOT_CONFIGURED', 'INVALID_CHECK_IN_DATE');\n  const checkOut = isoDate(raw.checkOut, 'CHECK_OUT_NOT_CONFIGURED', 'INVALID_CHECK_OUT_DATE');\n  if (Date.parse(`${checkOut}T00:00:00Z`) <= Date.parse(`${checkIn}T00:00:00Z`)) fail('CHECK_OUT_MUST_BE_AFTER_CHECK_IN');\n\n  const spreadsheetId = String(raw.spreadsheetId ?? '').trim();\n  const sheetName = String(raw.sheetName ?? '').trim();\n  if (!spreadsheetId || spreadsheetId === 'REPLACE_WITH_SPREADSHEET_ID') fail('SPREADSHEET_ID_NOT_CONFIGURED');\n  if (!/^[a-zA-Z0-9-_]{20,}$/.test(spreadsheetId)) fail('INVALID_SPREADSHEET_ID');\n  if (!sheetName || sheetName === 'REPLACE_WITH_SHEET_NAME' || sheetName.length > 100) fail('SHEET_NAME_NOT_CONFIGURED');\n\n  const adults = boundedInteger(raw.adults, 'INVALID_ADULTS_1_TO_30', 1, 30);\n  const rooms = boundedInteger(raw.rooms, 'INVALID_ROOMS_1_TO_30', 1, 30);\n  const maxProperties = boundedInteger(raw.maxProperties, 'INVALID_MAX_PROPERTIES_1_TO_1000', 1, MAX_PROPERTY_ROWS);\n  const maxPagesPerTarget = boundedInteger(raw.maxPagesPerTarget, 'INVALID_MAX_PAGES_1_TO_40', 1, 40);\n  const maxRequests = boundedInteger(raw.maxRequests, 'INVALID_MAX_REQUESTS_1_TO_250', 1, 250);\n  const maxRuntimeSecs = boundedInteger(raw.maxRuntimeSecs, 'INVALID_MAX_RUNTIME_30_TO_3600', 30, 3600);\n\n  const currency = upper(raw.currency || 'EUR');\n  const language = String(raw.language ?? 'en-gb').trim().toLowerCase();\n  const sortBy = String(raw.sortBy ?? 'popularity').trim();\n  if (!CURRENCIES.has(currency)) fail('INVALID_CURRENCY');\n  if (!LANGUAGES.has(language)) fail('INVALID_LANGUAGE');\n  if (!SORTS.has(sortBy)) fail('INVALID_SORT_ORDER');\n  const maxCostPerRunUsd = positiveFinite(raw.maxCostPerRunUsd, 'INVALID_MAX_CHARGE_CAP', 0.02139, 5);\n\n  return {\n    destination,\n    checkIn,\n    checkOut,\n    currency,\n    spreadsheetId,\n    sheetName,\n    targetCount: 1,\n    maxProperties,\n    maxCostPerRunUsd,\n    actorInput: {\n      destination,\n      checkIn,\n      checkOut,\n      adults,\n      rooms,\n      children: 0,\n      childrenAges: [],\n      flexibleDays: '0',\n      maxProperties,\n      mode: 'fast',\n      sortBy,\n      currency,\n      language,\n      maxPagesPerTarget,\n      maxRequests,\n      maxRuntimeSecs,\n      maxCostUsd: 0.25,\n      maxBuyerChargeUsd: maxCostPerRunUsd,\n      requestTimeoutSecs: 35,\n      maxRetries: 1,\n    },\n  };\n}\n\nfunction classifyBookingTerminalRun(run = {}, expectedBuildId = '') {\n  const status = upper(run.status);\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  if (run.error || !status) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'APIFY_RUN_NODE_ERROR' };\n  }\n  if (status !== 'SUCCEEDED') {\n    return { ok: false, state: 'ACTOR_FAILED_OR_ABORTED', run, runId, runUrl, reason: `ACTOR_STATUS_${status}` };\n  }\n  if (!expectedBuildId || String(run.buildId ?? '') !== String(expectedBuildId)) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'RUN_BUILD_ID_MISMATCH' };\n  }\n  if (!run.defaultKeyValueStoreId || !run.defaultDatasetId) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'RUN_STORAGE_IDS_MISSING' };\n  }\n  return { ok: true, state: 'RUN_SUCCEEDED', run, runId, runUrl };\n}\n\nfunction exactBookingBuyerCharge(billing) {\n  if (billing?.billingMode !== 'pay_per_event') fail('PPE_PRICING_NOT_ACTIVE');\n  const prices = billing.effectiveEventPricesUsd;\n  const counts = billing.counts;\n  if (!prices || typeof prices !== 'object' || !counts || typeof counts !== 'object') {\n    fail('AUTHORITATIVE_PPE_ACCOUNTING_MISSING');\n  }\n  const chargedCounts = {};\n  let total = 0;\n  for (const [eventName, eventCounts] of Object.entries(counts)) {\n    const charged = Number(eventCounts?.charged ?? 0);\n    if (!Number.isInteger(charged) || charged < 0) fail('INVALID_CHARGED_EVENT_COUNT');\n    chargedCounts[eventName] = charged;\n    if (charged === 0) continue;\n    const price = Number(prices[eventName]);\n    if (!Number.isFinite(price) || price < 0) fail('INVALID_EFFECTIVE_EVENT_PRICE');\n    total += charged * price;\n  }\n  return {\n    actualBuyerChargeUsd: Number(total.toFixed(12)),\n    chargedCounts,\n    effectivePricesUsd: { ...prices },\n  };\n}\n\nfunction parseBookingOutputRecord(record = {}, run = {}) {\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  const identity = { run, runId, runUrl };\n  const recordIsObject = record && typeof record === 'object' && !Array.isArray(record);\n  const data = recordIsObject && !record.error\n    ? (Object.prototype.hasOwnProperty.call(record, 'data') ? record.data : record)\n    : null;\n  if (!data || typeof data !== 'object' || Array.isArray(data) || data.error) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_SUMMARY_MISSING_OR_INVALID' };\n  }\n  let ppe;\n  try { ppe = exactBookingBuyerCharge(data.billing); } catch (error) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: error.message };\n  }\n  const outputRows = Number(data.propertyRows);\n  const targetCount = Number(data.targetCount);\n  if (!Number.isInteger(outputRows) || outputRows < 0 || outputRows > MAX_PROPERTY_ROWS) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_PROPERTY_ROW_COUNT_INVALID' };\n  }\n  const zeroCapacityGuard = targetCount === 0\n    && outputRows === 0\n    && upper(data.coverageState) === 'CAPPED'\n    && upper(data.coverageStatus) === 'CAPPED'\n    && data.stoppedByBillingGuard === true\n    && Array.isArray(data.perTarget)\n    && data.perTarget.length === 0;\n  if (!Number.isInteger(targetCount) || targetCount < 0 || !Array.isArray(data.perTarget)\n    || data.perTarget.length !== targetCount || (targetCount === 0 && !zeroCapacityGuard)) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_TARGET_CONTRACT_MISSING' };\n  }\n  if (Number(ppe.chargedCounts.fast_property_result ?? 0) !== outputRows) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'PPE_PROPERTY_DELIVERY_COUNT_MISMATCH' };\n  }\n  return {\n    ok: true,\n    state: 'SUMMARY_READY',\n    ...identity,\n    buyerChargeUsd: ppe.actualBuyerChargeUsd,\n    summary: {\n      schemaVersion: 2,\n      sourceRecordKey: 'OUTPUT',\n      status: upper(data.status),\n      coverageState: upper(data.coverageState),\n      coverageStatus: upper(data.coverageStatus),\n      outputRows,\n      expectedDatasetRows: outputRows + 1,\n      targetCount,\n      perTarget: data.perTarget,\n      warnings: Array.isArray(data.warnings) ? data.warnings.map(String) : [],\n      ppe: {\n        ...ppe,\n        deliveryEventLimitReached: data.billing?.eventLimitReached === true || data.stoppedByBillingGuard === true,\n      },\n    },\n  };\n}\n\nfunction planBookingDatasetPages(summary, pageSize = PAGE_SIZE) {\n  const total = boundedInteger(summary?.expectedDatasetRows, 'INVALID_SUMMARY_DATASET_ROWS', 1, MAX_DATASET_ROWS);\n  const size = boundedInteger(pageSize, 'INVALID_DATASET_PAGE_SIZE', 1, PAGE_SIZE);\n  return Array.from({ length: Math.ceil(total / size) }, (_, index) => ({\n    offset: index * size,\n    limit: Math.min(size, total - (index * size)),\n    expectedRows: total,\n  }));\n}\n\nfunction normalizedBookingPropertyPath(canonicalUrl) {\n  try {\n    const url = new URL(String(canonicalUrl ?? '').trim());\n    if (!/(^|\\.)booking\\.com$/i.test(url.hostname) || !/^\\/hotel\\//i.test(url.pathname)) return null;\n    return url.pathname.toLowerCase()\n      .replace(/\\/{2,}/g, '/')\n      .replace(/\\.[a-z]{2}(?:-[a-z]{2})?\\.html$/i, '.html');\n  } catch {\n    return null;\n  }\n}\n\nfunction bookingStableIdentity({ stablePropertyKey, canonicalUrl, propertyId }) {\n  const canonicalPath = normalizedBookingPropertyPath(canonicalUrl);\n  if (canonicalPath) return `booking_path:${canonicalPath}`;\n  const actorStableKey = String(stablePropertyKey ?? '').trim();\n  if (actorStableKey) return actorStableKey;\n  const id = String(propertyId ?? '').trim();\n  if (!id) fail('PROPERTY_ID_REQUIRED_FOR_STABLE_UPSERT');\n  return id;\n}\n\nfunction bookingUpsertKey({ stablePropertyKey, canonicalUrl, propertyId, checkIn, checkOut, currency }) {\n  const identity = bookingStableIdentity({ stablePropertyKey, canonicalUrl, propertyId });\n  if (!DATE_RE.test(String(checkIn ?? '')) || !DATE_RE.test(String(checkOut ?? ''))) fail('DATES_REQUIRED_FOR_STABLE_UPSERT');\n  const normalizedCurrency = upper(currency);\n  if (!CURRENCIES.has(normalizedCurrency)) fail('CURRENCY_REQUIRED_FOR_STABLE_UPSERT');\n  return `${identity}|${checkIn}|${checkOut}|${normalizedCurrency}`;\n}\n\nfunction scalar(value) {\n  if (value === null || value === undefined) return '';\n  if (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean') return value;\n  return JSON.stringify(value);\n}\n\nfunction prepareSheetRow(row, config, context) {\n  const search = row.searchParameters;\n  if (!search || typeof search !== 'object'\n    || String(search.checkIn ?? '') !== config.checkIn\n    || String(search.checkOut ?? '') !== config.checkOut\n    || upper(search.currency) !== config.currency) {\n    fail('PROPERTY_SEARCH_CONTEXT_MISMATCH');\n  }\n  const upsertKey = bookingUpsertKey({\n    stablePropertyKey: row.stablePropertyKey,\n    canonicalUrl: row.canonicalUrl,\n    propertyId: row.propertyId,\n    checkIn: config.checkIn,\n    checkOut: config.checkOut,\n    currency: config.currency,\n  });\n  return {\n    upsertKey,\n    stablePropertyKey: scalar(row.stablePropertyKey || bookingStableIdentity(row)),\n    propertyId: scalar(row.propertyId),\n    name: scalar(row.name),\n    checkIn: config.checkIn,\n    checkOut: config.checkOut,\n    requestedCurrency: config.currency,\n    resultCurrency: scalar(row.priceCurrency),\n    stayTotalAmount: scalar(row.stayTotalAmount),\n    stayNights: scalar(row.stayNights),\n    nightlyAmount: scalar(row.nightlyAmount),\n    guestReviewScore: scalar(row.guestReviewScore),\n    guestReviewCount: scalar(row.guestReviewCount),\n    stars: scalar(row.stars),\n    availabilityStatus: scalar(row.availabilityStatus),\n    locationText: scalar(row.locationText),\n    canonicalUrl: scalar(row.canonicalUrl),\n    priceBasis: scalar(row.priceBasis),\n    checkoutVerified: scalar(row.checkoutVerified),\n    collectedAt: scalar(row.collectedAt),\n    coverageState: context.coverageState,\n    workflowState: context.workflowState,\n    exactActorChargeUsd: context.buyerChargeUsd,\n    actorRunId: context.runId,\n  };\n}\n\nfunction reconcileBookingHotelsRun({ run = {}, summary = {}, rows = [], config = {} }) {\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  const datasetUrl = run.defaultDatasetId\n    ? `https://console.apify.com/storage/datasets/${run.defaultDatasetId}`\n    : runUrl;\n  const buyerChargeUsd = Number(summary?.ppe?.actualBuyerChargeUsd);\n  const base = {\n    run, summary, runId, runUrl, datasetUrl, buyerChargeUsd,\n    baselineState: 'NOT_APPLICABLE_STATELESS_SNAPSHOT',\n    unchangedState: 'NOT_CLAIMED_GOOGLE_SHEETS_UPSERT_ONLY',\n  };\n  if (!Number.isFinite(buyerChargeUsd) || buyerChargeUsd < 0) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'AUTHORITATIVE_BUYER_CHARGE_MISSING', shouldWrite: false, sheetRows: [] };\n  }\n  const zeroCapacityGuard = Number(summary.targetCount) === 0\n    && Number(summary.outputRows) === 0\n    && upper(summary.coverageState) === 'CAPPED'\n    && summary.ppe?.deliveryEventLimitReached === true;\n  if (!zeroCapacityGuard && Number(summary.targetCount) !== Number(config.targetCount)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'TARGET_RECONCILIATION_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n  if (rows.some((row) => row?.error)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DATASET_READ_ERROR', shouldWrite: false, sheetRows: [] };\n  }\n  const normalized = rows.filter((row) => row && typeof row === 'object' && Object.keys(row).length && !row.error);\n  const properties = normalized.filter((row) => row.recordType === 'property');\n  const coverageRows = normalized.filter((row) => row.recordType === 'coverage');\n  if (coverageRows.length !== 1 || normalized.length !== Number(summary.expectedDatasetRows)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DATASET_OR_COVERAGE_COUNT_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n  if (properties.length !== Number(summary.outputRows)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'PROPERTY_ROW_COUNT_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n\n  const coverageRecord = coverageRows[0];\n  if (Number(coverageRecord.targetCount) !== Number(summary.targetCount)\n    || upper(coverageRecord.coverageState) !== upper(summary.coverageState)\n    || upper(coverageRecord.coverageStatus) !== upper(summary.coverageStatus)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'OUTPUT_DATASET_COVERAGE_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n\n  if (zeroCapacityGuard) {\n    return {\n      ...base,\n      state: 'CAPPED',\n      reason: 'PPE_ALLOWANCE_ZERO_SOURCE_NOT_STARTED',\n      coverageState: 'CAPPED',\n      shouldWrite: false,\n      sheetRows: [],\n    };\n  }\n\n  const coverageState = upper(summary.coverageState || coverageRecord.coverageState);\n  const status = upper(summary.status || summary.coverageStatus);\n  if (['FAILED', 'BLOCKED'].includes(coverageState) || status === 'FAILED') {\n    return { ...base, state: 'FAILED', reason: 'SOURCE_FAILED_OR_BLOCKED', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (coverageState === 'PARTIAL' || status === 'PARTIAL') {\n    return { ...base, state: 'PARTIAL', reason: 'SOURCE_COVERAGE_PARTIAL', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  const workflowState = coverageState === 'CAPPED' || summary.ppe?.deliveryEventLimitReached === true\n    ? 'CAPPED'\n    : 'READY_TO_WRITE';\n\n  const seen = new Set();\n  const sheetRows = [];\n  try {\n    for (const property of properties) {\n      const row = prepareSheetRow(property, config, { coverageState, workflowState, buyerChargeUsd, runId });\n      if (seen.has(row.upsertKey)) continue;\n      seen.add(row.upsertKey);\n      sheetRows.push(row);\n    }\n  } catch (error) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: error.message, coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (sheetRows.length !== properties.length) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DUPLICATE_UPSERT_KEYS_IN_DATASET', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  const allEmpty = coverageState === 'EMPTY_CONFIRMED';\n  if (!sheetRows.length && allEmpty) {\n    return { ...base, state: 'EMPTY_VERIFIED', reason: 'SOURCE_VERIFIED_EMPTY', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (!sheetRows.length) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'ZERO_ROWS_WITHOUT_VERIFIED_EMPTY', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  return {\n    ...base,\n    state: workflowState,\n    reason: workflowState === 'CAPPED' ? 'BOUNDED_SHORTLIST_WRITTEN_WITH_CAP_DISCLOSED' : 'VERIFIED_SHORTLIST_READY',\n    coverageState,\n    shouldWrite: true,\n    sheetRows,\n  };\n}\n\nfunction renderBookingReceipt(result = {}, writtenRows = null) {\n  const labels = {\n    READY_TO_WRITE: 'Booking.com shortlist verified',\n    CAPPED: 'Booking.com bounded shortlist verified',\n    EMPTY_VERIFIED: 'Booking.com search verified empty',\n    PARTIAL: 'Booking.com search partial — no Sheet write',\n    FAILED: 'Booking.com source failed — no Sheet write',\n    ACTOR_FAILED_OR_ABORTED: 'Booking Hotels Actor failed or stopped',\n    WORKFLOW_NODE_FAILED: 'Workflow could not verify Booking.com output',\n  };\n  return {\n    state: result.state,\n    label: labels[result.state] ?? 'Booking.com shortlist result',\n    reason: result.reason ?? null,\n    rowsPrepared: Array.isArray(result.sheetRows) ? result.sheetRows.length : 0,\n    rowsWritten: Number.isInteger(writtenRows) ? writtenRows : null,\n    exactActorChargeUsd: Number.isFinite(Number(result.buyerChargeUsd)) ? Number(result.buyerChargeUsd) : null,\n    coverageState: result.coverageState ?? null,\n    baselineState: result.baselineState ?? 'NOT_APPLICABLE_STATELESS_SNAPSHOT',\n    unchangedState: result.unchangedState ?? 'NOT_CLAIMED_GOOGLE_SHEETS_UPSERT_ONLY',\n    runId: result.runId ?? null,\n    runUrl: result.runUrl ?? 'https://console.apify.com/actors/runs',\n    datasetUrl: result.datasetUrl ?? result.runUrl ?? 'https://console.apify.com/actors/runs',\n  };\n}\n\nconst BOOKING_HOTELS_WORKFLOW_LIMITS = Object.freeze({ maxPropertyRows: MAX_PROPERTY_ROWS, maxDatasetRows: MAX_DATASET_ROWS, pageSize: PAGE_SIZE });\n\n\nreturn [{ json: classifyBookingTerminalRun($input.first().json, 'w6usFEeFQcpi6ucKH') }];"
      },
      "id": "c5b8c6c9c809bef9456ae63b",
      "name": "Classify terminal status",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1136,
        100
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 2
          },
          "conditions": [
            {
              "id": "970efc04100b89831c9b82e3",
              "leftValue": "={{ $json.ok }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "2c0ad7bde9659eb19bf2684f",
      "name": "Run succeeded with storage IDs?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        1376,
        100
      ]
    },
    {
      "parameters": {
        "method": "GET",
        "url": "={{ $('Classify terminal status').first().json.run.output.runReport }}",
        "options": {
          "response": {
            "response": {
              "responseFormat": "json"
            }
          }
        }
      },
      "id": "38faeb1583f54c6054dcd24d",
      "name": "Get documented OUTPUT summary",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1616,
        -80
      ],
      "retryOnFail": false,
      "onError": "continueRegularOutput",
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// runtimeSourceSha256=a1f7a6f4e65bed3b976cad220ac0fc356239532e6427be18a40d9e7eb1f3ea5c\nconst CURRENCIES = new Set(['EUR', 'USD', 'GBP', 'CHF', 'CAD', 'AUD', 'JPY', 'PLN', 'AED', 'INR', 'TRY']);\nconst LANGUAGES = new Set(['en-gb', 'en-us', 'de', 'fr', 'es', 'it', 'nl', 'pt-pt', 'pl', 'ja', 'ko', 'zh-cn']);\nconst SORTS = new Set(['popularity', 'price_low', 'price_high', 'stars', 'distance', 'review_score']);\nconst MAX_PROPERTY_ROWS = 1000;\nconst MAX_DATASET_ROWS = 1001;\nconst PAGE_SIZE = 1000;\nconst DATE_RE = /^\\d{4}-\\d{2}-\\d{2}$/;\n\nfunction fail(code) {\n  throw new Error(code);\n}\n\nfunction upper(value) {\n  return String(value ?? '').trim().toUpperCase();\n}\n\nfunction boundedInteger(value, code, minimum, maximum) {\n  const parsed = Number(value);\n  if (!Number.isInteger(parsed) || parsed < minimum || parsed > maximum) fail(code);\n  return parsed;\n}\n\nfunction positiveFinite(value, code, minimum, maximum) {\n  const parsed = Number(value);\n  if (!Number.isFinite(parsed) || parsed < minimum || parsed > maximum) fail(code);\n  return parsed;\n}\n\nfunction isoDate(value, placeholderCode, invalidCode) {\n  const text = String(value ?? '').trim();\n  if (!text || text.startsWith('REPLACE_WITH_')) fail(placeholderCode);\n  if (!DATE_RE.test(text) || Number.isNaN(Date.parse(`${text}T00:00:00Z`))) fail(invalidCode);\n  return text;\n}\n\nfunction preflightBookingHotelsConfig(raw = {}) {\n  const destination = String(raw.destination ?? '').trim();\n  if (!destination || destination === 'REPLACE_WITH_DESTINATION' || destination.length > 160) fail('DESTINATION_NOT_CONFIGURED');\n  const checkIn = isoDate(raw.checkIn, 'CHECK_IN_NOT_CONFIGURED', 'INVALID_CHECK_IN_DATE');\n  const checkOut = isoDate(raw.checkOut, 'CHECK_OUT_NOT_CONFIGURED', 'INVALID_CHECK_OUT_DATE');\n  if (Date.parse(`${checkOut}T00:00:00Z`) <= Date.parse(`${checkIn}T00:00:00Z`)) fail('CHECK_OUT_MUST_BE_AFTER_CHECK_IN');\n\n  const spreadsheetId = String(raw.spreadsheetId ?? '').trim();\n  const sheetName = String(raw.sheetName ?? '').trim();\n  if (!spreadsheetId || spreadsheetId === 'REPLACE_WITH_SPREADSHEET_ID') fail('SPREADSHEET_ID_NOT_CONFIGURED');\n  if (!/^[a-zA-Z0-9-_]{20,}$/.test(spreadsheetId)) fail('INVALID_SPREADSHEET_ID');\n  if (!sheetName || sheetName === 'REPLACE_WITH_SHEET_NAME' || sheetName.length > 100) fail('SHEET_NAME_NOT_CONFIGURED');\n\n  const adults = boundedInteger(raw.adults, 'INVALID_ADULTS_1_TO_30', 1, 30);\n  const rooms = boundedInteger(raw.rooms, 'INVALID_ROOMS_1_TO_30', 1, 30);\n  const maxProperties = boundedInteger(raw.maxProperties, 'INVALID_MAX_PROPERTIES_1_TO_1000', 1, MAX_PROPERTY_ROWS);\n  const maxPagesPerTarget = boundedInteger(raw.maxPagesPerTarget, 'INVALID_MAX_PAGES_1_TO_40', 1, 40);\n  const maxRequests = boundedInteger(raw.maxRequests, 'INVALID_MAX_REQUESTS_1_TO_250', 1, 250);\n  const maxRuntimeSecs = boundedInteger(raw.maxRuntimeSecs, 'INVALID_MAX_RUNTIME_30_TO_3600', 30, 3600);\n\n  const currency = upper(raw.currency || 'EUR');\n  const language = String(raw.language ?? 'en-gb').trim().toLowerCase();\n  const sortBy = String(raw.sortBy ?? 'popularity').trim();\n  if (!CURRENCIES.has(currency)) fail('INVALID_CURRENCY');\n  if (!LANGUAGES.has(language)) fail('INVALID_LANGUAGE');\n  if (!SORTS.has(sortBy)) fail('INVALID_SORT_ORDER');\n  const maxCostPerRunUsd = positiveFinite(raw.maxCostPerRunUsd, 'INVALID_MAX_CHARGE_CAP', 0.02139, 5);\n\n  return {\n    destination,\n    checkIn,\n    checkOut,\n    currency,\n    spreadsheetId,\n    sheetName,\n    targetCount: 1,\n    maxProperties,\n    maxCostPerRunUsd,\n    actorInput: {\n      destination,\n      checkIn,\n      checkOut,\n      adults,\n      rooms,\n      children: 0,\n      childrenAges: [],\n      flexibleDays: '0',\n      maxProperties,\n      mode: 'fast',\n      sortBy,\n      currency,\n      language,\n      maxPagesPerTarget,\n      maxRequests,\n      maxRuntimeSecs,\n      maxCostUsd: 0.25,\n      maxBuyerChargeUsd: maxCostPerRunUsd,\n      requestTimeoutSecs: 35,\n      maxRetries: 1,\n    },\n  };\n}\n\nfunction classifyBookingTerminalRun(run = {}, expectedBuildId = '') {\n  const status = upper(run.status);\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  if (run.error || !status) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'APIFY_RUN_NODE_ERROR' };\n  }\n  if (status !== 'SUCCEEDED') {\n    return { ok: false, state: 'ACTOR_FAILED_OR_ABORTED', run, runId, runUrl, reason: `ACTOR_STATUS_${status}` };\n  }\n  if (!expectedBuildId || String(run.buildId ?? '') !== String(expectedBuildId)) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'RUN_BUILD_ID_MISMATCH' };\n  }\n  if (!run.defaultKeyValueStoreId || !run.defaultDatasetId) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'RUN_STORAGE_IDS_MISSING' };\n  }\n  return { ok: true, state: 'RUN_SUCCEEDED', run, runId, runUrl };\n}\n\nfunction exactBookingBuyerCharge(billing) {\n  if (billing?.billingMode !== 'pay_per_event') fail('PPE_PRICING_NOT_ACTIVE');\n  const prices = billing.effectiveEventPricesUsd;\n  const counts = billing.counts;\n  if (!prices || typeof prices !== 'object' || !counts || typeof counts !== 'object') {\n    fail('AUTHORITATIVE_PPE_ACCOUNTING_MISSING');\n  }\n  const chargedCounts = {};\n  let total = 0;\n  for (const [eventName, eventCounts] of Object.entries(counts)) {\n    const charged = Number(eventCounts?.charged ?? 0);\n    if (!Number.isInteger(charged) || charged < 0) fail('INVALID_CHARGED_EVENT_COUNT');\n    chargedCounts[eventName] = charged;\n    if (charged === 0) continue;\n    const price = Number(prices[eventName]);\n    if (!Number.isFinite(price) || price < 0) fail('INVALID_EFFECTIVE_EVENT_PRICE');\n    total += charged * price;\n  }\n  return {\n    actualBuyerChargeUsd: Number(total.toFixed(12)),\n    chargedCounts,\n    effectivePricesUsd: { ...prices },\n  };\n}\n\nfunction parseBookingOutputRecord(record = {}, run = {}) {\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  const identity = { run, runId, runUrl };\n  const recordIsObject = record && typeof record === 'object' && !Array.isArray(record);\n  const data = recordIsObject && !record.error\n    ? (Object.prototype.hasOwnProperty.call(record, 'data') ? record.data : record)\n    : null;\n  if (!data || typeof data !== 'object' || Array.isArray(data) || data.error) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_SUMMARY_MISSING_OR_INVALID' };\n  }\n  let ppe;\n  try { ppe = exactBookingBuyerCharge(data.billing); } catch (error) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: error.message };\n  }\n  const outputRows = Number(data.propertyRows);\n  const targetCount = Number(data.targetCount);\n  if (!Number.isInteger(outputRows) || outputRows < 0 || outputRows > MAX_PROPERTY_ROWS) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_PROPERTY_ROW_COUNT_INVALID' };\n  }\n  const zeroCapacityGuard = targetCount === 0\n    && outputRows === 0\n    && upper(data.coverageState) === 'CAPPED'\n    && upper(data.coverageStatus) === 'CAPPED'\n    && data.stoppedByBillingGuard === true\n    && Array.isArray(data.perTarget)\n    && data.perTarget.length === 0;\n  if (!Number.isInteger(targetCount) || targetCount < 0 || !Array.isArray(data.perTarget)\n    || data.perTarget.length !== targetCount || (targetCount === 0 && !zeroCapacityGuard)) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_TARGET_CONTRACT_MISSING' };\n  }\n  if (Number(ppe.chargedCounts.fast_property_result ?? 0) !== outputRows) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'PPE_PROPERTY_DELIVERY_COUNT_MISMATCH' };\n  }\n  return {\n    ok: true,\n    state: 'SUMMARY_READY',\n    ...identity,\n    buyerChargeUsd: ppe.actualBuyerChargeUsd,\n    summary: {\n      schemaVersion: 2,\n      sourceRecordKey: 'OUTPUT',\n      status: upper(data.status),\n      coverageState: upper(data.coverageState),\n      coverageStatus: upper(data.coverageStatus),\n      outputRows,\n      expectedDatasetRows: outputRows + 1,\n      targetCount,\n      perTarget: data.perTarget,\n      warnings: Array.isArray(data.warnings) ? data.warnings.map(String) : [],\n      ppe: {\n        ...ppe,\n        deliveryEventLimitReached: data.billing?.eventLimitReached === true || data.stoppedByBillingGuard === true,\n      },\n    },\n  };\n}\n\nfunction planBookingDatasetPages(summary, pageSize = PAGE_SIZE) {\n  const total = boundedInteger(summary?.expectedDatasetRows, 'INVALID_SUMMARY_DATASET_ROWS', 1, MAX_DATASET_ROWS);\n  const size = boundedInteger(pageSize, 'INVALID_DATASET_PAGE_SIZE', 1, PAGE_SIZE);\n  return Array.from({ length: Math.ceil(total / size) }, (_, index) => ({\n    offset: index * size,\n    limit: Math.min(size, total - (index * size)),\n    expectedRows: total,\n  }));\n}\n\nfunction normalizedBookingPropertyPath(canonicalUrl) {\n  try {\n    const url = new URL(String(canonicalUrl ?? '').trim());\n    if (!/(^|\\.)booking\\.com$/i.test(url.hostname) || !/^\\/hotel\\//i.test(url.pathname)) return null;\n    return url.pathname.toLowerCase()\n      .replace(/\\/{2,}/g, '/')\n      .replace(/\\.[a-z]{2}(?:-[a-z]{2})?\\.html$/i, '.html');\n  } catch {\n    return null;\n  }\n}\n\nfunction bookingStableIdentity({ stablePropertyKey, canonicalUrl, propertyId }) {\n  const canonicalPath = normalizedBookingPropertyPath(canonicalUrl);\n  if (canonicalPath) return `booking_path:${canonicalPath}`;\n  const actorStableKey = String(stablePropertyKey ?? '').trim();\n  if (actorStableKey) return actorStableKey;\n  const id = String(propertyId ?? '').trim();\n  if (!id) fail('PROPERTY_ID_REQUIRED_FOR_STABLE_UPSERT');\n  return id;\n}\n\nfunction bookingUpsertKey({ stablePropertyKey, canonicalUrl, propertyId, checkIn, checkOut, currency }) {\n  const identity = bookingStableIdentity({ stablePropertyKey, canonicalUrl, propertyId });\n  if (!DATE_RE.test(String(checkIn ?? '')) || !DATE_RE.test(String(checkOut ?? ''))) fail('DATES_REQUIRED_FOR_STABLE_UPSERT');\n  const normalizedCurrency = upper(currency);\n  if (!CURRENCIES.has(normalizedCurrency)) fail('CURRENCY_REQUIRED_FOR_STABLE_UPSERT');\n  return `${identity}|${checkIn}|${checkOut}|${normalizedCurrency}`;\n}\n\nfunction scalar(value) {\n  if (value === null || value === undefined) return '';\n  if (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean') return value;\n  return JSON.stringify(value);\n}\n\nfunction prepareSheetRow(row, config, context) {\n  const search = row.searchParameters;\n  if (!search || typeof search !== 'object'\n    || String(search.checkIn ?? '') !== config.checkIn\n    || String(search.checkOut ?? '') !== config.checkOut\n    || upper(search.currency) !== config.currency) {\n    fail('PROPERTY_SEARCH_CONTEXT_MISMATCH');\n  }\n  const upsertKey = bookingUpsertKey({\n    stablePropertyKey: row.stablePropertyKey,\n    canonicalUrl: row.canonicalUrl,\n    propertyId: row.propertyId,\n    checkIn: config.checkIn,\n    checkOut: config.checkOut,\n    currency: config.currency,\n  });\n  return {\n    upsertKey,\n    stablePropertyKey: scalar(row.stablePropertyKey || bookingStableIdentity(row)),\n    propertyId: scalar(row.propertyId),\n    name: scalar(row.name),\n    checkIn: config.checkIn,\n    checkOut: config.checkOut,\n    requestedCurrency: config.currency,\n    resultCurrency: scalar(row.priceCurrency),\n    stayTotalAmount: scalar(row.stayTotalAmount),\n    stayNights: scalar(row.stayNights),\n    nightlyAmount: scalar(row.nightlyAmount),\n    guestReviewScore: scalar(row.guestReviewScore),\n    guestReviewCount: scalar(row.guestReviewCount),\n    stars: scalar(row.stars),\n    availabilityStatus: scalar(row.availabilityStatus),\n    locationText: scalar(row.locationText),\n    canonicalUrl: scalar(row.canonicalUrl),\n    priceBasis: scalar(row.priceBasis),\n    checkoutVerified: scalar(row.checkoutVerified),\n    collectedAt: scalar(row.collectedAt),\n    coverageState: context.coverageState,\n    workflowState: context.workflowState,\n    exactActorChargeUsd: context.buyerChargeUsd,\n    actorRunId: context.runId,\n  };\n}\n\nfunction reconcileBookingHotelsRun({ run = {}, summary = {}, rows = [], config = {} }) {\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  const datasetUrl = run.defaultDatasetId\n    ? `https://console.apify.com/storage/datasets/${run.defaultDatasetId}`\n    : runUrl;\n  const buyerChargeUsd = Number(summary?.ppe?.actualBuyerChargeUsd);\n  const base = {\n    run, summary, runId, runUrl, datasetUrl, buyerChargeUsd,\n    baselineState: 'NOT_APPLICABLE_STATELESS_SNAPSHOT',\n    unchangedState: 'NOT_CLAIMED_GOOGLE_SHEETS_UPSERT_ONLY',\n  };\n  if (!Number.isFinite(buyerChargeUsd) || buyerChargeUsd < 0) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'AUTHORITATIVE_BUYER_CHARGE_MISSING', shouldWrite: false, sheetRows: [] };\n  }\n  const zeroCapacityGuard = Number(summary.targetCount) === 0\n    && Number(summary.outputRows) === 0\n    && upper(summary.coverageState) === 'CAPPED'\n    && summary.ppe?.deliveryEventLimitReached === true;\n  if (!zeroCapacityGuard && Number(summary.targetCount) !== Number(config.targetCount)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'TARGET_RECONCILIATION_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n  if (rows.some((row) => row?.error)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DATASET_READ_ERROR', shouldWrite: false, sheetRows: [] };\n  }\n  const normalized = rows.filter((row) => row && typeof row === 'object' && Object.keys(row).length && !row.error);\n  const properties = normalized.filter((row) => row.recordType === 'property');\n  const coverageRows = normalized.filter((row) => row.recordType === 'coverage');\n  if (coverageRows.length !== 1 || normalized.length !== Number(summary.expectedDatasetRows)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DATASET_OR_COVERAGE_COUNT_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n  if (properties.length !== Number(summary.outputRows)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'PROPERTY_ROW_COUNT_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n\n  const coverageRecord = coverageRows[0];\n  if (Number(coverageRecord.targetCount) !== Number(summary.targetCount)\n    || upper(coverageRecord.coverageState) !== upper(summary.coverageState)\n    || upper(coverageRecord.coverageStatus) !== upper(summary.coverageStatus)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'OUTPUT_DATASET_COVERAGE_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n\n  if (zeroCapacityGuard) {\n    return {\n      ...base,\n      state: 'CAPPED',\n      reason: 'PPE_ALLOWANCE_ZERO_SOURCE_NOT_STARTED',\n      coverageState: 'CAPPED',\n      shouldWrite: false,\n      sheetRows: [],\n    };\n  }\n\n  const coverageState = upper(summary.coverageState || coverageRecord.coverageState);\n  const status = upper(summary.status || summary.coverageStatus);\n  if (['FAILED', 'BLOCKED'].includes(coverageState) || status === 'FAILED') {\n    return { ...base, state: 'FAILED', reason: 'SOURCE_FAILED_OR_BLOCKED', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (coverageState === 'PARTIAL' || status === 'PARTIAL') {\n    return { ...base, state: 'PARTIAL', reason: 'SOURCE_COVERAGE_PARTIAL', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  const workflowState = coverageState === 'CAPPED' || summary.ppe?.deliveryEventLimitReached === true\n    ? 'CAPPED'\n    : 'READY_TO_WRITE';\n\n  const seen = new Set();\n  const sheetRows = [];\n  try {\n    for (const property of properties) {\n      const row = prepareSheetRow(property, config, { coverageState, workflowState, buyerChargeUsd, runId });\n      if (seen.has(row.upsertKey)) continue;\n      seen.add(row.upsertKey);\n      sheetRows.push(row);\n    }\n  } catch (error) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: error.message, coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (sheetRows.length !== properties.length) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DUPLICATE_UPSERT_KEYS_IN_DATASET', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  const allEmpty = coverageState === 'EMPTY_CONFIRMED';\n  if (!sheetRows.length && allEmpty) {\n    return { ...base, state: 'EMPTY_VERIFIED', reason: 'SOURCE_VERIFIED_EMPTY', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (!sheetRows.length) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'ZERO_ROWS_WITHOUT_VERIFIED_EMPTY', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  return {\n    ...base,\n    state: workflowState,\n    reason: workflowState === 'CAPPED' ? 'BOUNDED_SHORTLIST_WRITTEN_WITH_CAP_DISCLOSED' : 'VERIFIED_SHORTLIST_READY',\n    coverageState,\n    shouldWrite: true,\n    sheetRows,\n  };\n}\n\nfunction renderBookingReceipt(result = {}, writtenRows = null) {\n  const labels = {\n    READY_TO_WRITE: 'Booking.com shortlist verified',\n    CAPPED: 'Booking.com bounded shortlist verified',\n    EMPTY_VERIFIED: 'Booking.com search verified empty',\n    PARTIAL: 'Booking.com search partial — no Sheet write',\n    FAILED: 'Booking.com source failed — no Sheet write',\n    ACTOR_FAILED_OR_ABORTED: 'Booking Hotels Actor failed or stopped',\n    WORKFLOW_NODE_FAILED: 'Workflow could not verify Booking.com output',\n  };\n  return {\n    state: result.state,\n    label: labels[result.state] ?? 'Booking.com shortlist result',\n    reason: result.reason ?? null,\n    rowsPrepared: Array.isArray(result.sheetRows) ? result.sheetRows.length : 0,\n    rowsWritten: Number.isInteger(writtenRows) ? writtenRows : null,\n    exactActorChargeUsd: Number.isFinite(Number(result.buyerChargeUsd)) ? Number(result.buyerChargeUsd) : null,\n    coverageState: result.coverageState ?? null,\n    baselineState: result.baselineState ?? 'NOT_APPLICABLE_STATELESS_SNAPSHOT',\n    unchangedState: result.unchangedState ?? 'NOT_CLAIMED_GOOGLE_SHEETS_UPSERT_ONLY',\n    runId: result.runId ?? null,\n    runUrl: result.runUrl ?? 'https://console.apify.com/actors/runs',\n    datasetUrl: result.datasetUrl ?? result.runUrl ?? 'https://console.apify.com/actors/runs',\n  };\n}\n\nconst BOOKING_HOTELS_WORKFLOW_LIMITS = Object.freeze({ maxPropertyRows: MAX_PROPERTY_ROWS, maxDatasetRows: MAX_DATASET_ROWS, pageSize: PAGE_SIZE });\n\n\nconst run = $('Classify terminal status').first().json.run; return [{ json: parseBookingOutputRecord($input.first().json, run) }];"
      },
      "id": "5d588f399833afba75df5261",
      "name": "Normalize authoritative summary",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1840,
        -80
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 2
          },
          "conditions": [
            {
              "id": "877181a2a021073650249730",
              "leftValue": "={{ $json.ok }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "d586d92750a59ae4b20fa475",
      "name": "Summary valid?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        2072,
        -80
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// runtimeSourceSha256=a1f7a6f4e65bed3b976cad220ac0fc356239532e6427be18a40d9e7eb1f3ea5c\nconst CURRENCIES = new Set(['EUR', 'USD', 'GBP', 'CHF', 'CAD', 'AUD', 'JPY', 'PLN', 'AED', 'INR', 'TRY']);\nconst LANGUAGES = new Set(['en-gb', 'en-us', 'de', 'fr', 'es', 'it', 'nl', 'pt-pt', 'pl', 'ja', 'ko', 'zh-cn']);\nconst SORTS = new Set(['popularity', 'price_low', 'price_high', 'stars', 'distance', 'review_score']);\nconst MAX_PROPERTY_ROWS = 1000;\nconst MAX_DATASET_ROWS = 1001;\nconst PAGE_SIZE = 1000;\nconst DATE_RE = /^\\d{4}-\\d{2}-\\d{2}$/;\n\nfunction fail(code) {\n  throw new Error(code);\n}\n\nfunction upper(value) {\n  return String(value ?? '').trim().toUpperCase();\n}\n\nfunction boundedInteger(value, code, minimum, maximum) {\n  const parsed = Number(value);\n  if (!Number.isInteger(parsed) || parsed < minimum || parsed > maximum) fail(code);\n  return parsed;\n}\n\nfunction positiveFinite(value, code, minimum, maximum) {\n  const parsed = Number(value);\n  if (!Number.isFinite(parsed) || parsed < minimum || parsed > maximum) fail(code);\n  return parsed;\n}\n\nfunction isoDate(value, placeholderCode, invalidCode) {\n  const text = String(value ?? '').trim();\n  if (!text || text.startsWith('REPLACE_WITH_')) fail(placeholderCode);\n  if (!DATE_RE.test(text) || Number.isNaN(Date.parse(`${text}T00:00:00Z`))) fail(invalidCode);\n  return text;\n}\n\nfunction preflightBookingHotelsConfig(raw = {}) {\n  const destination = String(raw.destination ?? '').trim();\n  if (!destination || destination === 'REPLACE_WITH_DESTINATION' || destination.length > 160) fail('DESTINATION_NOT_CONFIGURED');\n  const checkIn = isoDate(raw.checkIn, 'CHECK_IN_NOT_CONFIGURED', 'INVALID_CHECK_IN_DATE');\n  const checkOut = isoDate(raw.checkOut, 'CHECK_OUT_NOT_CONFIGURED', 'INVALID_CHECK_OUT_DATE');\n  if (Date.parse(`${checkOut}T00:00:00Z`) <= Date.parse(`${checkIn}T00:00:00Z`)) fail('CHECK_OUT_MUST_BE_AFTER_CHECK_IN');\n\n  const spreadsheetId = String(raw.spreadsheetId ?? '').trim();\n  const sheetName = String(raw.sheetName ?? '').trim();\n  if (!spreadsheetId || spreadsheetId === 'REPLACE_WITH_SPREADSHEET_ID') fail('SPREADSHEET_ID_NOT_CONFIGURED');\n  if (!/^[a-zA-Z0-9-_]{20,}$/.test(spreadsheetId)) fail('INVALID_SPREADSHEET_ID');\n  if (!sheetName || sheetName === 'REPLACE_WITH_SHEET_NAME' || sheetName.length > 100) fail('SHEET_NAME_NOT_CONFIGURED');\n\n  const adults = boundedInteger(raw.adults, 'INVALID_ADULTS_1_TO_30', 1, 30);\n  const rooms = boundedInteger(raw.rooms, 'INVALID_ROOMS_1_TO_30', 1, 30);\n  const maxProperties = boundedInteger(raw.maxProperties, 'INVALID_MAX_PROPERTIES_1_TO_1000', 1, MAX_PROPERTY_ROWS);\n  const maxPagesPerTarget = boundedInteger(raw.maxPagesPerTarget, 'INVALID_MAX_PAGES_1_TO_40', 1, 40);\n  const maxRequests = boundedInteger(raw.maxRequests, 'INVALID_MAX_REQUESTS_1_TO_250', 1, 250);\n  const maxRuntimeSecs = boundedInteger(raw.maxRuntimeSecs, 'INVALID_MAX_RUNTIME_30_TO_3600', 30, 3600);\n\n  const currency = upper(raw.currency || 'EUR');\n  const language = String(raw.language ?? 'en-gb').trim().toLowerCase();\n  const sortBy = String(raw.sortBy ?? 'popularity').trim();\n  if (!CURRENCIES.has(currency)) fail('INVALID_CURRENCY');\n  if (!LANGUAGES.has(language)) fail('INVALID_LANGUAGE');\n  if (!SORTS.has(sortBy)) fail('INVALID_SORT_ORDER');\n  const maxCostPerRunUsd = positiveFinite(raw.maxCostPerRunUsd, 'INVALID_MAX_CHARGE_CAP', 0.02139, 5);\n\n  return {\n    destination,\n    checkIn,\n    checkOut,\n    currency,\n    spreadsheetId,\n    sheetName,\n    targetCount: 1,\n    maxProperties,\n    maxCostPerRunUsd,\n    actorInput: {\n      destination,\n      checkIn,\n      checkOut,\n      adults,\n      rooms,\n      children: 0,\n      childrenAges: [],\n      flexibleDays: '0',\n      maxProperties,\n      mode: 'fast',\n      sortBy,\n      currency,\n      language,\n      maxPagesPerTarget,\n      maxRequests,\n      maxRuntimeSecs,\n      maxCostUsd: 0.25,\n      maxBuyerChargeUsd: maxCostPerRunUsd,\n      requestTimeoutSecs: 35,\n      maxRetries: 1,\n    },\n  };\n}\n\nfunction classifyBookingTerminalRun(run = {}, expectedBuildId = '') {\n  const status = upper(run.status);\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  if (run.error || !status) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'APIFY_RUN_NODE_ERROR' };\n  }\n  if (status !== 'SUCCEEDED') {\n    return { ok: false, state: 'ACTOR_FAILED_OR_ABORTED', run, runId, runUrl, reason: `ACTOR_STATUS_${status}` };\n  }\n  if (!expectedBuildId || String(run.buildId ?? '') !== String(expectedBuildId)) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'RUN_BUILD_ID_MISMATCH' };\n  }\n  if (!run.defaultKeyValueStoreId || !run.defaultDatasetId) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'RUN_STORAGE_IDS_MISSING' };\n  }\n  return { ok: true, state: 'RUN_SUCCEEDED', run, runId, runUrl };\n}\n\nfunction exactBookingBuyerCharge(billing) {\n  if (billing?.billingMode !== 'pay_per_event') fail('PPE_PRICING_NOT_ACTIVE');\n  const prices = billing.effectiveEventPricesUsd;\n  const counts = billing.counts;\n  if (!prices || typeof prices !== 'object' || !counts || typeof counts !== 'object') {\n    fail('AUTHORITATIVE_PPE_ACCOUNTING_MISSING');\n  }\n  const chargedCounts = {};\n  let total = 0;\n  for (const [eventName, eventCounts] of Object.entries(counts)) {\n    const charged = Number(eventCounts?.charged ?? 0);\n    if (!Number.isInteger(charged) || charged < 0) fail('INVALID_CHARGED_EVENT_COUNT');\n    chargedCounts[eventName] = charged;\n    if (charged === 0) continue;\n    const price = Number(prices[eventName]);\n    if (!Number.isFinite(price) || price < 0) fail('INVALID_EFFECTIVE_EVENT_PRICE');\n    total += charged * price;\n  }\n  return {\n    actualBuyerChargeUsd: Number(total.toFixed(12)),\n    chargedCounts,\n    effectivePricesUsd: { ...prices },\n  };\n}\n\nfunction parseBookingOutputRecord(record = {}, run = {}) {\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  const identity = { run, runId, runUrl };\n  const recordIsObject = record && typeof record === 'object' && !Array.isArray(record);\n  const data = recordIsObject && !record.error\n    ? (Object.prototype.hasOwnProperty.call(record, 'data') ? record.data : record)\n    : null;\n  if (!data || typeof data !== 'object' || Array.isArray(data) || data.error) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_SUMMARY_MISSING_OR_INVALID' };\n  }\n  let ppe;\n  try { ppe = exactBookingBuyerCharge(data.billing); } catch (error) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: error.message };\n  }\n  const outputRows = Number(data.propertyRows);\n  const targetCount = Number(data.targetCount);\n  if (!Number.isInteger(outputRows) || outputRows < 0 || outputRows > MAX_PROPERTY_ROWS) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_PROPERTY_ROW_COUNT_INVALID' };\n  }\n  const zeroCapacityGuard = targetCount === 0\n    && outputRows === 0\n    && upper(data.coverageState) === 'CAPPED'\n    && upper(data.coverageStatus) === 'CAPPED'\n    && data.stoppedByBillingGuard === true\n    && Array.isArray(data.perTarget)\n    && data.perTarget.length === 0;\n  if (!Number.isInteger(targetCount) || targetCount < 0 || !Array.isArray(data.perTarget)\n    || data.perTarget.length !== targetCount || (targetCount === 0 && !zeroCapacityGuard)) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_TARGET_CONTRACT_MISSING' };\n  }\n  if (Number(ppe.chargedCounts.fast_property_result ?? 0) !== outputRows) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'PPE_PROPERTY_DELIVERY_COUNT_MISMATCH' };\n  }\n  return {\n    ok: true,\n    state: 'SUMMARY_READY',\n    ...identity,\n    buyerChargeUsd: ppe.actualBuyerChargeUsd,\n    summary: {\n      schemaVersion: 2,\n      sourceRecordKey: 'OUTPUT',\n      status: upper(data.status),\n      coverageState: upper(data.coverageState),\n      coverageStatus: upper(data.coverageStatus),\n      outputRows,\n      expectedDatasetRows: outputRows + 1,\n      targetCount,\n      perTarget: data.perTarget,\n      warnings: Array.isArray(data.warnings) ? data.warnings.map(String) : [],\n      ppe: {\n        ...ppe,\n        deliveryEventLimitReached: data.billing?.eventLimitReached === true || data.stoppedByBillingGuard === true,\n      },\n    },\n  };\n}\n\nfunction planBookingDatasetPages(summary, pageSize = PAGE_SIZE) {\n  const total = boundedInteger(summary?.expectedDatasetRows, 'INVALID_SUMMARY_DATASET_ROWS', 1, MAX_DATASET_ROWS);\n  const size = boundedInteger(pageSize, 'INVALID_DATASET_PAGE_SIZE', 1, PAGE_SIZE);\n  return Array.from({ length: Math.ceil(total / size) }, (_, index) => ({\n    offset: index * size,\n    limit: Math.min(size, total - (index * size)),\n    expectedRows: total,\n  }));\n}\n\nfunction normalizedBookingPropertyPath(canonicalUrl) {\n  try {\n    const url = new URL(String(canonicalUrl ?? '').trim());\n    if (!/(^|\\.)booking\\.com$/i.test(url.hostname) || !/^\\/hotel\\//i.test(url.pathname)) return null;\n    return url.pathname.toLowerCase()\n      .replace(/\\/{2,}/g, '/')\n      .replace(/\\.[a-z]{2}(?:-[a-z]{2})?\\.html$/i, '.html');\n  } catch {\n    return null;\n  }\n}\n\nfunction bookingStableIdentity({ stablePropertyKey, canonicalUrl, propertyId }) {\n  const canonicalPath = normalizedBookingPropertyPath(canonicalUrl);\n  if (canonicalPath) return `booking_path:${canonicalPath}`;\n  const actorStableKey = String(stablePropertyKey ?? '').trim();\n  if (actorStableKey) return actorStableKey;\n  const id = String(propertyId ?? '').trim();\n  if (!id) fail('PROPERTY_ID_REQUIRED_FOR_STABLE_UPSERT');\n  return id;\n}\n\nfunction bookingUpsertKey({ stablePropertyKey, canonicalUrl, propertyId, checkIn, checkOut, currency }) {\n  const identity = bookingStableIdentity({ stablePropertyKey, canonicalUrl, propertyId });\n  if (!DATE_RE.test(String(checkIn ?? '')) || !DATE_RE.test(String(checkOut ?? ''))) fail('DATES_REQUIRED_FOR_STABLE_UPSERT');\n  const normalizedCurrency = upper(currency);\n  if (!CURRENCIES.has(normalizedCurrency)) fail('CURRENCY_REQUIRED_FOR_STABLE_UPSERT');\n  return `${identity}|${checkIn}|${checkOut}|${normalizedCurrency}`;\n}\n\nfunction scalar(value) {\n  if (value === null || value === undefined) return '';\n  if (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean') return value;\n  return JSON.stringify(value);\n}\n\nfunction prepareSheetRow(row, config, context) {\n  const search = row.searchParameters;\n  if (!search || typeof search !== 'object'\n    || String(search.checkIn ?? '') !== config.checkIn\n    || String(search.checkOut ?? '') !== config.checkOut\n    || upper(search.currency) !== config.currency) {\n    fail('PROPERTY_SEARCH_CONTEXT_MISMATCH');\n  }\n  const upsertKey = bookingUpsertKey({\n    stablePropertyKey: row.stablePropertyKey,\n    canonicalUrl: row.canonicalUrl,\n    propertyId: row.propertyId,\n    checkIn: config.checkIn,\n    checkOut: config.checkOut,\n    currency: config.currency,\n  });\n  return {\n    upsertKey,\n    stablePropertyKey: scalar(row.stablePropertyKey || bookingStableIdentity(row)),\n    propertyId: scalar(row.propertyId),\n    name: scalar(row.name),\n    checkIn: config.checkIn,\n    checkOut: config.checkOut,\n    requestedCurrency: config.currency,\n    resultCurrency: scalar(row.priceCurrency),\n    stayTotalAmount: scalar(row.stayTotalAmount),\n    stayNights: scalar(row.stayNights),\n    nightlyAmount: scalar(row.nightlyAmount),\n    guestReviewScore: scalar(row.guestReviewScore),\n    guestReviewCount: scalar(row.guestReviewCount),\n    stars: scalar(row.stars),\n    availabilityStatus: scalar(row.availabilityStatus),\n    locationText: scalar(row.locationText),\n    canonicalUrl: scalar(row.canonicalUrl),\n    priceBasis: scalar(row.priceBasis),\n    checkoutVerified: scalar(row.checkoutVerified),\n    collectedAt: scalar(row.collectedAt),\n    coverageState: context.coverageState,\n    workflowState: context.workflowState,\n    exactActorChargeUsd: context.buyerChargeUsd,\n    actorRunId: context.runId,\n  };\n}\n\nfunction reconcileBookingHotelsRun({ run = {}, summary = {}, rows = [], config = {} }) {\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  const datasetUrl = run.defaultDatasetId\n    ? `https://console.apify.com/storage/datasets/${run.defaultDatasetId}`\n    : runUrl;\n  const buyerChargeUsd = Number(summary?.ppe?.actualBuyerChargeUsd);\n  const base = {\n    run, summary, runId, runUrl, datasetUrl, buyerChargeUsd,\n    baselineState: 'NOT_APPLICABLE_STATELESS_SNAPSHOT',\n    unchangedState: 'NOT_CLAIMED_GOOGLE_SHEETS_UPSERT_ONLY',\n  };\n  if (!Number.isFinite(buyerChargeUsd) || buyerChargeUsd < 0) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'AUTHORITATIVE_BUYER_CHARGE_MISSING', shouldWrite: false, sheetRows: [] };\n  }\n  const zeroCapacityGuard = Number(summary.targetCount) === 0\n    && Number(summary.outputRows) === 0\n    && upper(summary.coverageState) === 'CAPPED'\n    && summary.ppe?.deliveryEventLimitReached === true;\n  if (!zeroCapacityGuard && Number(summary.targetCount) !== Number(config.targetCount)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'TARGET_RECONCILIATION_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n  if (rows.some((row) => row?.error)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DATASET_READ_ERROR', shouldWrite: false, sheetRows: [] };\n  }\n  const normalized = rows.filter((row) => row && typeof row === 'object' && Object.keys(row).length && !row.error);\n  const properties = normalized.filter((row) => row.recordType === 'property');\n  const coverageRows = normalized.filter((row) => row.recordType === 'coverage');\n  if (coverageRows.length !== 1 || normalized.length !== Number(summary.expectedDatasetRows)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DATASET_OR_COVERAGE_COUNT_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n  if (properties.length !== Number(summary.outputRows)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'PROPERTY_ROW_COUNT_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n\n  const coverageRecord = coverageRows[0];\n  if (Number(coverageRecord.targetCount) !== Number(summary.targetCount)\n    || upper(coverageRecord.coverageState) !== upper(summary.coverageState)\n    || upper(coverageRecord.coverageStatus) !== upper(summary.coverageStatus)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'OUTPUT_DATASET_COVERAGE_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n\n  if (zeroCapacityGuard) {\n    return {\n      ...base,\n      state: 'CAPPED',\n      reason: 'PPE_ALLOWANCE_ZERO_SOURCE_NOT_STARTED',\n      coverageState: 'CAPPED',\n      shouldWrite: false,\n      sheetRows: [],\n    };\n  }\n\n  const coverageState = upper(summary.coverageState || coverageRecord.coverageState);\n  const status = upper(summary.status || summary.coverageStatus);\n  if (['FAILED', 'BLOCKED'].includes(coverageState) || status === 'FAILED') {\n    return { ...base, state: 'FAILED', reason: 'SOURCE_FAILED_OR_BLOCKED', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (coverageState === 'PARTIAL' || status === 'PARTIAL') {\n    return { ...base, state: 'PARTIAL', reason: 'SOURCE_COVERAGE_PARTIAL', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  const workflowState = coverageState === 'CAPPED' || summary.ppe?.deliveryEventLimitReached === true\n    ? 'CAPPED'\n    : 'READY_TO_WRITE';\n\n  const seen = new Set();\n  const sheetRows = [];\n  try {\n    for (const property of properties) {\n      const row = prepareSheetRow(property, config, { coverageState, workflowState, buyerChargeUsd, runId });\n      if (seen.has(row.upsertKey)) continue;\n      seen.add(row.upsertKey);\n      sheetRows.push(row);\n    }\n  } catch (error) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: error.message, coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (sheetRows.length !== properties.length) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DUPLICATE_UPSERT_KEYS_IN_DATASET', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  const allEmpty = coverageState === 'EMPTY_CONFIRMED';\n  if (!sheetRows.length && allEmpty) {\n    return { ...base, state: 'EMPTY_VERIFIED', reason: 'SOURCE_VERIFIED_EMPTY', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (!sheetRows.length) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'ZERO_ROWS_WITHOUT_VERIFIED_EMPTY', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  return {\n    ...base,\n    state: workflowState,\n    reason: workflowState === 'CAPPED' ? 'BOUNDED_SHORTLIST_WRITTEN_WITH_CAP_DISCLOSED' : 'VERIFIED_SHORTLIST_READY',\n    coverageState,\n    shouldWrite: true,\n    sheetRows,\n  };\n}\n\nfunction renderBookingReceipt(result = {}, writtenRows = null) {\n  const labels = {\n    READY_TO_WRITE: 'Booking.com shortlist verified',\n    CAPPED: 'Booking.com bounded shortlist verified',\n    EMPTY_VERIFIED: 'Booking.com search verified empty',\n    PARTIAL: 'Booking.com search partial — no Sheet write',\n    FAILED: 'Booking.com source failed — no Sheet write',\n    ACTOR_FAILED_OR_ABORTED: 'Booking Hotels Actor failed or stopped',\n    WORKFLOW_NODE_FAILED: 'Workflow could not verify Booking.com output',\n  };\n  return {\n    state: result.state,\n    label: labels[result.state] ?? 'Booking.com shortlist result',\n    reason: result.reason ?? null,\n    rowsPrepared: Array.isArray(result.sheetRows) ? result.sheetRows.length : 0,\n    rowsWritten: Number.isInteger(writtenRows) ? writtenRows : null,\n    exactActorChargeUsd: Number.isFinite(Number(result.buyerChargeUsd)) ? Number(result.buyerChargeUsd) : null,\n    coverageState: result.coverageState ?? null,\n    baselineState: result.baselineState ?? 'NOT_APPLICABLE_STATELESS_SNAPSHOT',\n    unchangedState: result.unchangedState ?? 'NOT_CLAIMED_GOOGLE_SHEETS_UPSERT_ONLY',\n    runId: result.runId ?? null,\n    runUrl: result.runUrl ?? 'https://console.apify.com/actors/runs',\n    datasetUrl: result.datasetUrl ?? result.runUrl ?? 'https://console.apify.com/actors/runs',\n  };\n}\n\nconst BOOKING_HOTELS_WORKFLOW_LIMITS = Object.freeze({ maxPropertyRows: MAX_PROPERTY_ROWS, maxDatasetRows: MAX_DATASET_ROWS, pageSize: PAGE_SIZE });\n\n\nreturn planBookingDatasetPages($input.first().json.summary).map((page) => ({ json: page }));"
      },
      "id": "046637a41d4f1885e5bdd3a5",
      "name": "Plan complete dataset pages",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2304,
        -80
      ]
    },
    {
      "parameters": {
        "authentication": "apifyOAuth2Api",
        "resource": "Datasets",
        "operation": "Get items",
        "datasetId": "={{ $('Run exact Booking Hotels Actor once').first().json.defaultDatasetId }}",
        "offset": "={{ $json.offset }}",
        "limit": "={{ $json.limit }}",
        "options": {}
      },
      "id": "d6a129bebab5d1b2987305b7",
      "name": "Get every property and coverage page",
      "type": "@apify/n8n-nodes-apify.apify",
      "typeVersion": 1,
      "position": [
        2536,
        -80
      ],
      "retryOnFail": false,
      "onError": "continueRegularOutput",
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// runtimeSourceSha256=a1f7a6f4e65bed3b976cad220ac0fc356239532e6427be18a40d9e7eb1f3ea5c\nconst CURRENCIES = new Set(['EUR', 'USD', 'GBP', 'CHF', 'CAD', 'AUD', 'JPY', 'PLN', 'AED', 'INR', 'TRY']);\nconst LANGUAGES = new Set(['en-gb', 'en-us', 'de', 'fr', 'es', 'it', 'nl', 'pt-pt', 'pl', 'ja', 'ko', 'zh-cn']);\nconst SORTS = new Set(['popularity', 'price_low', 'price_high', 'stars', 'distance', 'review_score']);\nconst MAX_PROPERTY_ROWS = 1000;\nconst MAX_DATASET_ROWS = 1001;\nconst PAGE_SIZE = 1000;\nconst DATE_RE = /^\\d{4}-\\d{2}-\\d{2}$/;\n\nfunction fail(code) {\n  throw new Error(code);\n}\n\nfunction upper(value) {\n  return String(value ?? '').trim().toUpperCase();\n}\n\nfunction boundedInteger(value, code, minimum, maximum) {\n  const parsed = Number(value);\n  if (!Number.isInteger(parsed) || parsed < minimum || parsed > maximum) fail(code);\n  return parsed;\n}\n\nfunction positiveFinite(value, code, minimum, maximum) {\n  const parsed = Number(value);\n  if (!Number.isFinite(parsed) || parsed < minimum || parsed > maximum) fail(code);\n  return parsed;\n}\n\nfunction isoDate(value, placeholderCode, invalidCode) {\n  const text = String(value ?? '').trim();\n  if (!text || text.startsWith('REPLACE_WITH_')) fail(placeholderCode);\n  if (!DATE_RE.test(text) || Number.isNaN(Date.parse(`${text}T00:00:00Z`))) fail(invalidCode);\n  return text;\n}\n\nfunction preflightBookingHotelsConfig(raw = {}) {\n  const destination = String(raw.destination ?? '').trim();\n  if (!destination || destination === 'REPLACE_WITH_DESTINATION' || destination.length > 160) fail('DESTINATION_NOT_CONFIGURED');\n  const checkIn = isoDate(raw.checkIn, 'CHECK_IN_NOT_CONFIGURED', 'INVALID_CHECK_IN_DATE');\n  const checkOut = isoDate(raw.checkOut, 'CHECK_OUT_NOT_CONFIGURED', 'INVALID_CHECK_OUT_DATE');\n  if (Date.parse(`${checkOut}T00:00:00Z`) <= Date.parse(`${checkIn}T00:00:00Z`)) fail('CHECK_OUT_MUST_BE_AFTER_CHECK_IN');\n\n  const spreadsheetId = String(raw.spreadsheetId ?? '').trim();\n  const sheetName = String(raw.sheetName ?? '').trim();\n  if (!spreadsheetId || spreadsheetId === 'REPLACE_WITH_SPREADSHEET_ID') fail('SPREADSHEET_ID_NOT_CONFIGURED');\n  if (!/^[a-zA-Z0-9-_]{20,}$/.test(spreadsheetId)) fail('INVALID_SPREADSHEET_ID');\n  if (!sheetName || sheetName === 'REPLACE_WITH_SHEET_NAME' || sheetName.length > 100) fail('SHEET_NAME_NOT_CONFIGURED');\n\n  const adults = boundedInteger(raw.adults, 'INVALID_ADULTS_1_TO_30', 1, 30);\n  const rooms = boundedInteger(raw.rooms, 'INVALID_ROOMS_1_TO_30', 1, 30);\n  const maxProperties = boundedInteger(raw.maxProperties, 'INVALID_MAX_PROPERTIES_1_TO_1000', 1, MAX_PROPERTY_ROWS);\n  const maxPagesPerTarget = boundedInteger(raw.maxPagesPerTarget, 'INVALID_MAX_PAGES_1_TO_40', 1, 40);\n  const maxRequests = boundedInteger(raw.maxRequests, 'INVALID_MAX_REQUESTS_1_TO_250', 1, 250);\n  const maxRuntimeSecs = boundedInteger(raw.maxRuntimeSecs, 'INVALID_MAX_RUNTIME_30_TO_3600', 30, 3600);\n\n  const currency = upper(raw.currency || 'EUR');\n  const language = String(raw.language ?? 'en-gb').trim().toLowerCase();\n  const sortBy = String(raw.sortBy ?? 'popularity').trim();\n  if (!CURRENCIES.has(currency)) fail('INVALID_CURRENCY');\n  if (!LANGUAGES.has(language)) fail('INVALID_LANGUAGE');\n  if (!SORTS.has(sortBy)) fail('INVALID_SORT_ORDER');\n  const maxCostPerRunUsd = positiveFinite(raw.maxCostPerRunUsd, 'INVALID_MAX_CHARGE_CAP', 0.02139, 5);\n\n  return {\n    destination,\n    checkIn,\n    checkOut,\n    currency,\n    spreadsheetId,\n    sheetName,\n    targetCount: 1,\n    maxProperties,\n    maxCostPerRunUsd,\n    actorInput: {\n      destination,\n      checkIn,\n      checkOut,\n      adults,\n      rooms,\n      children: 0,\n      childrenAges: [],\n      flexibleDays: '0',\n      maxProperties,\n      mode: 'fast',\n      sortBy,\n      currency,\n      language,\n      maxPagesPerTarget,\n      maxRequests,\n      maxRuntimeSecs,\n      maxCostUsd: 0.25,\n      maxBuyerChargeUsd: maxCostPerRunUsd,\n      requestTimeoutSecs: 35,\n      maxRetries: 1,\n    },\n  };\n}\n\nfunction classifyBookingTerminalRun(run = {}, expectedBuildId = '') {\n  const status = upper(run.status);\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  if (run.error || !status) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'APIFY_RUN_NODE_ERROR' };\n  }\n  if (status !== 'SUCCEEDED') {\n    return { ok: false, state: 'ACTOR_FAILED_OR_ABORTED', run, runId, runUrl, reason: `ACTOR_STATUS_${status}` };\n  }\n  if (!expectedBuildId || String(run.buildId ?? '') !== String(expectedBuildId)) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'RUN_BUILD_ID_MISMATCH' };\n  }\n  if (!run.defaultKeyValueStoreId || !run.defaultDatasetId) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'RUN_STORAGE_IDS_MISSING' };\n  }\n  return { ok: true, state: 'RUN_SUCCEEDED', run, runId, runUrl };\n}\n\nfunction exactBookingBuyerCharge(billing) {\n  if (billing?.billingMode !== 'pay_per_event') fail('PPE_PRICING_NOT_ACTIVE');\n  const prices = billing.effectiveEventPricesUsd;\n  const counts = billing.counts;\n  if (!prices || typeof prices !== 'object' || !counts || typeof counts !== 'object') {\n    fail('AUTHORITATIVE_PPE_ACCOUNTING_MISSING');\n  }\n  const chargedCounts = {};\n  let total = 0;\n  for (const [eventName, eventCounts] of Object.entries(counts)) {\n    const charged = Number(eventCounts?.charged ?? 0);\n    if (!Number.isInteger(charged) || charged < 0) fail('INVALID_CHARGED_EVENT_COUNT');\n    chargedCounts[eventName] = charged;\n    if (charged === 0) continue;\n    const price = Number(prices[eventName]);\n    if (!Number.isFinite(price) || price < 0) fail('INVALID_EFFECTIVE_EVENT_PRICE');\n    total += charged * price;\n  }\n  return {\n    actualBuyerChargeUsd: Number(total.toFixed(12)),\n    chargedCounts,\n    effectivePricesUsd: { ...prices },\n  };\n}\n\nfunction parseBookingOutputRecord(record = {}, run = {}) {\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  const identity = { run, runId, runUrl };\n  const recordIsObject = record && typeof record === 'object' && !Array.isArray(record);\n  const data = recordIsObject && !record.error\n    ? (Object.prototype.hasOwnProperty.call(record, 'data') ? record.data : record)\n    : null;\n  if (!data || typeof data !== 'object' || Array.isArray(data) || data.error) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_SUMMARY_MISSING_OR_INVALID' };\n  }\n  let ppe;\n  try { ppe = exactBookingBuyerCharge(data.billing); } catch (error) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: error.message };\n  }\n  const outputRows = Number(data.propertyRows);\n  const targetCount = Number(data.targetCount);\n  if (!Number.isInteger(outputRows) || outputRows < 0 || outputRows > MAX_PROPERTY_ROWS) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_PROPERTY_ROW_COUNT_INVALID' };\n  }\n  const zeroCapacityGuard = targetCount === 0\n    && outputRows === 0\n    && upper(data.coverageState) === 'CAPPED'\n    && upper(data.coverageStatus) === 'CAPPED'\n    && data.stoppedByBillingGuard === true\n    && Array.isArray(data.perTarget)\n    && data.perTarget.length === 0;\n  if (!Number.isInteger(targetCount) || targetCount < 0 || !Array.isArray(data.perTarget)\n    || data.perTarget.length !== targetCount || (targetCount === 0 && !zeroCapacityGuard)) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_TARGET_CONTRACT_MISSING' };\n  }\n  if (Number(ppe.chargedCounts.fast_property_result ?? 0) !== outputRows) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'PPE_PROPERTY_DELIVERY_COUNT_MISMATCH' };\n  }\n  return {\n    ok: true,\n    state: 'SUMMARY_READY',\n    ...identity,\n    buyerChargeUsd: ppe.actualBuyerChargeUsd,\n    summary: {\n      schemaVersion: 2,\n      sourceRecordKey: 'OUTPUT',\n      status: upper(data.status),\n      coverageState: upper(data.coverageState),\n      coverageStatus: upper(data.coverageStatus),\n      outputRows,\n      expectedDatasetRows: outputRows + 1,\n      targetCount,\n      perTarget: data.perTarget,\n      warnings: Array.isArray(data.warnings) ? data.warnings.map(String) : [],\n      ppe: {\n        ...ppe,\n        deliveryEventLimitReached: data.billing?.eventLimitReached === true || data.stoppedByBillingGuard === true,\n      },\n    },\n  };\n}\n\nfunction planBookingDatasetPages(summary, pageSize = PAGE_SIZE) {\n  const total = boundedInteger(summary?.expectedDatasetRows, 'INVALID_SUMMARY_DATASET_ROWS', 1, MAX_DATASET_ROWS);\n  const size = boundedInteger(pageSize, 'INVALID_DATASET_PAGE_SIZE', 1, PAGE_SIZE);\n  return Array.from({ length: Math.ceil(total / size) }, (_, index) => ({\n    offset: index * size,\n    limit: Math.min(size, total - (index * size)),\n    expectedRows: total,\n  }));\n}\n\nfunction normalizedBookingPropertyPath(canonicalUrl) {\n  try {\n    const url = new URL(String(canonicalUrl ?? '').trim());\n    if (!/(^|\\.)booking\\.com$/i.test(url.hostname) || !/^\\/hotel\\//i.test(url.pathname)) return null;\n    return url.pathname.toLowerCase()\n      .replace(/\\/{2,}/g, '/')\n      .replace(/\\.[a-z]{2}(?:-[a-z]{2})?\\.html$/i, '.html');\n  } catch {\n    return null;\n  }\n}\n\nfunction bookingStableIdentity({ stablePropertyKey, canonicalUrl, propertyId }) {\n  const canonicalPath = normalizedBookingPropertyPath(canonicalUrl);\n  if (canonicalPath) return `booking_path:${canonicalPath}`;\n  const actorStableKey = String(stablePropertyKey ?? '').trim();\n  if (actorStableKey) return actorStableKey;\n  const id = String(propertyId ?? '').trim();\n  if (!id) fail('PROPERTY_ID_REQUIRED_FOR_STABLE_UPSERT');\n  return id;\n}\n\nfunction bookingUpsertKey({ stablePropertyKey, canonicalUrl, propertyId, checkIn, checkOut, currency }) {\n  const identity = bookingStableIdentity({ stablePropertyKey, canonicalUrl, propertyId });\n  if (!DATE_RE.test(String(checkIn ?? '')) || !DATE_RE.test(String(checkOut ?? ''))) fail('DATES_REQUIRED_FOR_STABLE_UPSERT');\n  const normalizedCurrency = upper(currency);\n  if (!CURRENCIES.has(normalizedCurrency)) fail('CURRENCY_REQUIRED_FOR_STABLE_UPSERT');\n  return `${identity}|${checkIn}|${checkOut}|${normalizedCurrency}`;\n}\n\nfunction scalar(value) {\n  if (value === null || value === undefined) return '';\n  if (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean') return value;\n  return JSON.stringify(value);\n}\n\nfunction prepareSheetRow(row, config, context) {\n  const search = row.searchParameters;\n  if (!search || typeof search !== 'object'\n    || String(search.checkIn ?? '') !== config.checkIn\n    || String(search.checkOut ?? '') !== config.checkOut\n    || upper(search.currency) !== config.currency) {\n    fail('PROPERTY_SEARCH_CONTEXT_MISMATCH');\n  }\n  const upsertKey = bookingUpsertKey({\n    stablePropertyKey: row.stablePropertyKey,\n    canonicalUrl: row.canonicalUrl,\n    propertyId: row.propertyId,\n    checkIn: config.checkIn,\n    checkOut: config.checkOut,\n    currency: config.currency,\n  });\n  return {\n    upsertKey,\n    stablePropertyKey: scalar(row.stablePropertyKey || bookingStableIdentity(row)),\n    propertyId: scalar(row.propertyId),\n    name: scalar(row.name),\n    checkIn: config.checkIn,\n    checkOut: config.checkOut,\n    requestedCurrency: config.currency,\n    resultCurrency: scalar(row.priceCurrency),\n    stayTotalAmount: scalar(row.stayTotalAmount),\n    stayNights: scalar(row.stayNights),\n    nightlyAmount: scalar(row.nightlyAmount),\n    guestReviewScore: scalar(row.guestReviewScore),\n    guestReviewCount: scalar(row.guestReviewCount),\n    stars: scalar(row.stars),\n    availabilityStatus: scalar(row.availabilityStatus),\n    locationText: scalar(row.locationText),\n    canonicalUrl: scalar(row.canonicalUrl),\n    priceBasis: scalar(row.priceBasis),\n    checkoutVerified: scalar(row.checkoutVerified),\n    collectedAt: scalar(row.collectedAt),\n    coverageState: context.coverageState,\n    workflowState: context.workflowState,\n    exactActorChargeUsd: context.buyerChargeUsd,\n    actorRunId: context.runId,\n  };\n}\n\nfunction reconcileBookingHotelsRun({ run = {}, summary = {}, rows = [], config = {} }) {\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  const datasetUrl = run.defaultDatasetId\n    ? `https://console.apify.com/storage/datasets/${run.defaultDatasetId}`\n    : runUrl;\n  const buyerChargeUsd = Number(summary?.ppe?.actualBuyerChargeUsd);\n  const base = {\n    run, summary, runId, runUrl, datasetUrl, buyerChargeUsd,\n    baselineState: 'NOT_APPLICABLE_STATELESS_SNAPSHOT',\n    unchangedState: 'NOT_CLAIMED_GOOGLE_SHEETS_UPSERT_ONLY',\n  };\n  if (!Number.isFinite(buyerChargeUsd) || buyerChargeUsd < 0) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'AUTHORITATIVE_BUYER_CHARGE_MISSING', shouldWrite: false, sheetRows: [] };\n  }\n  const zeroCapacityGuard = Number(summary.targetCount) === 0\n    && Number(summary.outputRows) === 0\n    && upper(summary.coverageState) === 'CAPPED'\n    && summary.ppe?.deliveryEventLimitReached === true;\n  if (!zeroCapacityGuard && Number(summary.targetCount) !== Number(config.targetCount)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'TARGET_RECONCILIATION_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n  if (rows.some((row) => row?.error)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DATASET_READ_ERROR', shouldWrite: false, sheetRows: [] };\n  }\n  const normalized = rows.filter((row) => row && typeof row === 'object' && Object.keys(row).length && !row.error);\n  const properties = normalized.filter((row) => row.recordType === 'property');\n  const coverageRows = normalized.filter((row) => row.recordType === 'coverage');\n  if (coverageRows.length !== 1 || normalized.length !== Number(summary.expectedDatasetRows)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DATASET_OR_COVERAGE_COUNT_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n  if (properties.length !== Number(summary.outputRows)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'PROPERTY_ROW_COUNT_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n\n  const coverageRecord = coverageRows[0];\n  if (Number(coverageRecord.targetCount) !== Number(summary.targetCount)\n    || upper(coverageRecord.coverageState) !== upper(summary.coverageState)\n    || upper(coverageRecord.coverageStatus) !== upper(summary.coverageStatus)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'OUTPUT_DATASET_COVERAGE_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n\n  if (zeroCapacityGuard) {\n    return {\n      ...base,\n      state: 'CAPPED',\n      reason: 'PPE_ALLOWANCE_ZERO_SOURCE_NOT_STARTED',\n      coverageState: 'CAPPED',\n      shouldWrite: false,\n      sheetRows: [],\n    };\n  }\n\n  const coverageState = upper(summary.coverageState || coverageRecord.coverageState);\n  const status = upper(summary.status || summary.coverageStatus);\n  if (['FAILED', 'BLOCKED'].includes(coverageState) || status === 'FAILED') {\n    return { ...base, state: 'FAILED', reason: 'SOURCE_FAILED_OR_BLOCKED', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (coverageState === 'PARTIAL' || status === 'PARTIAL') {\n    return { ...base, state: 'PARTIAL', reason: 'SOURCE_COVERAGE_PARTIAL', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  const workflowState = coverageState === 'CAPPED' || summary.ppe?.deliveryEventLimitReached === true\n    ? 'CAPPED'\n    : 'READY_TO_WRITE';\n\n  const seen = new Set();\n  const sheetRows = [];\n  try {\n    for (const property of properties) {\n      const row = prepareSheetRow(property, config, { coverageState, workflowState, buyerChargeUsd, runId });\n      if (seen.has(row.upsertKey)) continue;\n      seen.add(row.upsertKey);\n      sheetRows.push(row);\n    }\n  } catch (error) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: error.message, coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (sheetRows.length !== properties.length) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DUPLICATE_UPSERT_KEYS_IN_DATASET', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  const allEmpty = coverageState === 'EMPTY_CONFIRMED';\n  if (!sheetRows.length && allEmpty) {\n    return { ...base, state: 'EMPTY_VERIFIED', reason: 'SOURCE_VERIFIED_EMPTY', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (!sheetRows.length) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'ZERO_ROWS_WITHOUT_VERIFIED_EMPTY', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  return {\n    ...base,\n    state: workflowState,\n    reason: workflowState === 'CAPPED' ? 'BOUNDED_SHORTLIST_WRITTEN_WITH_CAP_DISCLOSED' : 'VERIFIED_SHORTLIST_READY',\n    coverageState,\n    shouldWrite: true,\n    sheetRows,\n  };\n}\n\nfunction renderBookingReceipt(result = {}, writtenRows = null) {\n  const labels = {\n    READY_TO_WRITE: 'Booking.com shortlist verified',\n    CAPPED: 'Booking.com bounded shortlist verified',\n    EMPTY_VERIFIED: 'Booking.com search verified empty',\n    PARTIAL: 'Booking.com search partial — no Sheet write',\n    FAILED: 'Booking.com source failed — no Sheet write',\n    ACTOR_FAILED_OR_ABORTED: 'Booking Hotels Actor failed or stopped',\n    WORKFLOW_NODE_FAILED: 'Workflow could not verify Booking.com output',\n  };\n  return {\n    state: result.state,\n    label: labels[result.state] ?? 'Booking.com shortlist result',\n    reason: result.reason ?? null,\n    rowsPrepared: Array.isArray(result.sheetRows) ? result.sheetRows.length : 0,\n    rowsWritten: Number.isInteger(writtenRows) ? writtenRows : null,\n    exactActorChargeUsd: Number.isFinite(Number(result.buyerChargeUsd)) ? Number(result.buyerChargeUsd) : null,\n    coverageState: result.coverageState ?? null,\n    baselineState: result.baselineState ?? 'NOT_APPLICABLE_STATELESS_SNAPSHOT',\n    unchangedState: result.unchangedState ?? 'NOT_CLAIMED_GOOGLE_SHEETS_UPSERT_ONLY',\n    runId: result.runId ?? null,\n    runUrl: result.runUrl ?? 'https://console.apify.com/actors/runs',\n    datasetUrl: result.datasetUrl ?? result.runUrl ?? 'https://console.apify.com/actors/runs',\n  };\n}\n\nconst BOOKING_HOTELS_WORKFLOW_LIMITS = Object.freeze({ maxPropertyRows: MAX_PROPERTY_ROWS, maxDatasetRows: MAX_DATASET_ROWS, pageSize: PAGE_SIZE });\n\n\nconst run = $('Classify terminal status').first().json.run;\nconst summary = $('Normalize authoritative summary').first().json.summary;\nconst config = $('Fail-closed preflight').first().json;\nconst rows = $input.all().map((item) => item.json);\nreturn [{ json: reconcileBookingHotelsRun({ run, summary, rows, config }) }];"
      },
      "id": "a2753c05f5d498a9888e49dc",
      "name": "Reconcile rows coverage and exact charge",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3008,
        -80
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 2
          },
          "conditions": [
            {
              "id": "8076e716aa033b997c81e920",
              "leftValue": "={{ $json.shouldWrite }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "c4f78fa6a51591a78cd13f3d",
      "name": "Verified rows ready for upsert?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [
        3248,
        80
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// runtimeSourceSha256=a1f7a6f4e65bed3b976cad220ac0fc356239532e6427be18a40d9e7eb1f3ea5c\nconst CURRENCIES = new Set(['EUR', 'USD', 'GBP', 'CHF', 'CAD', 'AUD', 'JPY', 'PLN', 'AED', 'INR', 'TRY']);\nconst LANGUAGES = new Set(['en-gb', 'en-us', 'de', 'fr', 'es', 'it', 'nl', 'pt-pt', 'pl', 'ja', 'ko', 'zh-cn']);\nconst SORTS = new Set(['popularity', 'price_low', 'price_high', 'stars', 'distance', 'review_score']);\nconst MAX_PROPERTY_ROWS = 1000;\nconst MAX_DATASET_ROWS = 1001;\nconst PAGE_SIZE = 1000;\nconst DATE_RE = /^\\d{4}-\\d{2}-\\d{2}$/;\n\nfunction fail(code) {\n  throw new Error(code);\n}\n\nfunction upper(value) {\n  return String(value ?? '').trim().toUpperCase();\n}\n\nfunction boundedInteger(value, code, minimum, maximum) {\n  const parsed = Number(value);\n  if (!Number.isInteger(parsed) || parsed < minimum || parsed > maximum) fail(code);\n  return parsed;\n}\n\nfunction positiveFinite(value, code, minimum, maximum) {\n  const parsed = Number(value);\n  if (!Number.isFinite(parsed) || parsed < minimum || parsed > maximum) fail(code);\n  return parsed;\n}\n\nfunction isoDate(value, placeholderCode, invalidCode) {\n  const text = String(value ?? '').trim();\n  if (!text || text.startsWith('REPLACE_WITH_')) fail(placeholderCode);\n  if (!DATE_RE.test(text) || Number.isNaN(Date.parse(`${text}T00:00:00Z`))) fail(invalidCode);\n  return text;\n}\n\nfunction preflightBookingHotelsConfig(raw = {}) {\n  const destination = String(raw.destination ?? '').trim();\n  if (!destination || destination === 'REPLACE_WITH_DESTINATION' || destination.length > 160) fail('DESTINATION_NOT_CONFIGURED');\n  const checkIn = isoDate(raw.checkIn, 'CHECK_IN_NOT_CONFIGURED', 'INVALID_CHECK_IN_DATE');\n  const checkOut = isoDate(raw.checkOut, 'CHECK_OUT_NOT_CONFIGURED', 'INVALID_CHECK_OUT_DATE');\n  if (Date.parse(`${checkOut}T00:00:00Z`) <= Date.parse(`${checkIn}T00:00:00Z`)) fail('CHECK_OUT_MUST_BE_AFTER_CHECK_IN');\n\n  const spreadsheetId = String(raw.spreadsheetId ?? '').trim();\n  const sheetName = String(raw.sheetName ?? '').trim();\n  if (!spreadsheetId || spreadsheetId === 'REPLACE_WITH_SPREADSHEET_ID') fail('SPREADSHEET_ID_NOT_CONFIGURED');\n  if (!/^[a-zA-Z0-9-_]{20,}$/.test(spreadsheetId)) fail('INVALID_SPREADSHEET_ID');\n  if (!sheetName || sheetName === 'REPLACE_WITH_SHEET_NAME' || sheetName.length > 100) fail('SHEET_NAME_NOT_CONFIGURED');\n\n  const adults = boundedInteger(raw.adults, 'INVALID_ADULTS_1_TO_30', 1, 30);\n  const rooms = boundedInteger(raw.rooms, 'INVALID_ROOMS_1_TO_30', 1, 30);\n  const maxProperties = boundedInteger(raw.maxProperties, 'INVALID_MAX_PROPERTIES_1_TO_1000', 1, MAX_PROPERTY_ROWS);\n  const maxPagesPerTarget = boundedInteger(raw.maxPagesPerTarget, 'INVALID_MAX_PAGES_1_TO_40', 1, 40);\n  const maxRequests = boundedInteger(raw.maxRequests, 'INVALID_MAX_REQUESTS_1_TO_250', 1, 250);\n  const maxRuntimeSecs = boundedInteger(raw.maxRuntimeSecs, 'INVALID_MAX_RUNTIME_30_TO_3600', 30, 3600);\n\n  const currency = upper(raw.currency || 'EUR');\n  const language = String(raw.language ?? 'en-gb').trim().toLowerCase();\n  const sortBy = String(raw.sortBy ?? 'popularity').trim();\n  if (!CURRENCIES.has(currency)) fail('INVALID_CURRENCY');\n  if (!LANGUAGES.has(language)) fail('INVALID_LANGUAGE');\n  if (!SORTS.has(sortBy)) fail('INVALID_SORT_ORDER');\n  const maxCostPerRunUsd = positiveFinite(raw.maxCostPerRunUsd, 'INVALID_MAX_CHARGE_CAP', 0.02139, 5);\n\n  return {\n    destination,\n    checkIn,\n    checkOut,\n    currency,\n    spreadsheetId,\n    sheetName,\n    targetCount: 1,\n    maxProperties,\n    maxCostPerRunUsd,\n    actorInput: {\n      destination,\n      checkIn,\n      checkOut,\n      adults,\n      rooms,\n      children: 0,\n      childrenAges: [],\n      flexibleDays: '0',\n      maxProperties,\n      mode: 'fast',\n      sortBy,\n      currency,\n      language,\n      maxPagesPerTarget,\n      maxRequests,\n      maxRuntimeSecs,\n      maxCostUsd: 0.25,\n      maxBuyerChargeUsd: maxCostPerRunUsd,\n      requestTimeoutSecs: 35,\n      maxRetries: 1,\n    },\n  };\n}\n\nfunction classifyBookingTerminalRun(run = {}, expectedBuildId = '') {\n  const status = upper(run.status);\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  if (run.error || !status) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'APIFY_RUN_NODE_ERROR' };\n  }\n  if (status !== 'SUCCEEDED') {\n    return { ok: false, state: 'ACTOR_FAILED_OR_ABORTED', run, runId, runUrl, reason: `ACTOR_STATUS_${status}` };\n  }\n  if (!expectedBuildId || String(run.buildId ?? '') !== String(expectedBuildId)) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'RUN_BUILD_ID_MISMATCH' };\n  }\n  if (!run.defaultKeyValueStoreId || !run.defaultDatasetId) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'RUN_STORAGE_IDS_MISSING' };\n  }\n  return { ok: true, state: 'RUN_SUCCEEDED', run, runId, runUrl };\n}\n\nfunction exactBookingBuyerCharge(billing) {\n  if (billing?.billingMode !== 'pay_per_event') fail('PPE_PRICING_NOT_ACTIVE');\n  const prices = billing.effectiveEventPricesUsd;\n  const counts = billing.counts;\n  if (!prices || typeof prices !== 'object' || !counts || typeof counts !== 'object') {\n    fail('AUTHORITATIVE_PPE_ACCOUNTING_MISSING');\n  }\n  const chargedCounts = {};\n  let total = 0;\n  for (const [eventName, eventCounts] of Object.entries(counts)) {\n    const charged = Number(eventCounts?.charged ?? 0);\n    if (!Number.isInteger(charged) || charged < 0) fail('INVALID_CHARGED_EVENT_COUNT');\n    chargedCounts[eventName] = charged;\n    if (charged === 0) continue;\n    const price = Number(prices[eventName]);\n    if (!Number.isFinite(price) || price < 0) fail('INVALID_EFFECTIVE_EVENT_PRICE');\n    total += charged * price;\n  }\n  return {\n    actualBuyerChargeUsd: Number(total.toFixed(12)),\n    chargedCounts,\n    effectivePricesUsd: { ...prices },\n  };\n}\n\nfunction parseBookingOutputRecord(record = {}, run = {}) {\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  const identity = { run, runId, runUrl };\n  const recordIsObject = record && typeof record === 'object' && !Array.isArray(record);\n  const data = recordIsObject && !record.error\n    ? (Object.prototype.hasOwnProperty.call(record, 'data') ? record.data : record)\n    : null;\n  if (!data || typeof data !== 'object' || Array.isArray(data) || data.error) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_SUMMARY_MISSING_OR_INVALID' };\n  }\n  let ppe;\n  try { ppe = exactBookingBuyerCharge(data.billing); } catch (error) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: error.message };\n  }\n  const outputRows = Number(data.propertyRows);\n  const targetCount = Number(data.targetCount);\n  if (!Number.isInteger(outputRows) || outputRows < 0 || outputRows > MAX_PROPERTY_ROWS) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_PROPERTY_ROW_COUNT_INVALID' };\n  }\n  const zeroCapacityGuard = targetCount === 0\n    && outputRows === 0\n    && upper(data.coverageState) === 'CAPPED'\n    && upper(data.coverageStatus) === 'CAPPED'\n    && data.stoppedByBillingGuard === true\n    && Array.isArray(data.perTarget)\n    && data.perTarget.length === 0;\n  if (!Number.isInteger(targetCount) || targetCount < 0 || !Array.isArray(data.perTarget)\n    || data.perTarget.length !== targetCount || (targetCount === 0 && !zeroCapacityGuard)) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_TARGET_CONTRACT_MISSING' };\n  }\n  if (Number(ppe.chargedCounts.fast_property_result ?? 0) !== outputRows) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'PPE_PROPERTY_DELIVERY_COUNT_MISMATCH' };\n  }\n  return {\n    ok: true,\n    state: 'SUMMARY_READY',\n    ...identity,\n    buyerChargeUsd: ppe.actualBuyerChargeUsd,\n    summary: {\n      schemaVersion: 2,\n      sourceRecordKey: 'OUTPUT',\n      status: upper(data.status),\n      coverageState: upper(data.coverageState),\n      coverageStatus: upper(data.coverageStatus),\n      outputRows,\n      expectedDatasetRows: outputRows + 1,\n      targetCount,\n      perTarget: data.perTarget,\n      warnings: Array.isArray(data.warnings) ? data.warnings.map(String) : [],\n      ppe: {\n        ...ppe,\n        deliveryEventLimitReached: data.billing?.eventLimitReached === true || data.stoppedByBillingGuard === true,\n      },\n    },\n  };\n}\n\nfunction planBookingDatasetPages(summary, pageSize = PAGE_SIZE) {\n  const total = boundedInteger(summary?.expectedDatasetRows, 'INVALID_SUMMARY_DATASET_ROWS', 1, MAX_DATASET_ROWS);\n  const size = boundedInteger(pageSize, 'INVALID_DATASET_PAGE_SIZE', 1, PAGE_SIZE);\n  return Array.from({ length: Math.ceil(total / size) }, (_, index) => ({\n    offset: index * size,\n    limit: Math.min(size, total - (index * size)),\n    expectedRows: total,\n  }));\n}\n\nfunction normalizedBookingPropertyPath(canonicalUrl) {\n  try {\n    const url = new URL(String(canonicalUrl ?? '').trim());\n    if (!/(^|\\.)booking\\.com$/i.test(url.hostname) || !/^\\/hotel\\//i.test(url.pathname)) return null;\n    return url.pathname.toLowerCase()\n      .replace(/\\/{2,}/g, '/')\n      .replace(/\\.[a-z]{2}(?:-[a-z]{2})?\\.html$/i, '.html');\n  } catch {\n    return null;\n  }\n}\n\nfunction bookingStableIdentity({ stablePropertyKey, canonicalUrl, propertyId }) {\n  const canonicalPath = normalizedBookingPropertyPath(canonicalUrl);\n  if (canonicalPath) return `booking_path:${canonicalPath}`;\n  const actorStableKey = String(stablePropertyKey ?? '').trim();\n  if (actorStableKey) return actorStableKey;\n  const id = String(propertyId ?? '').trim();\n  if (!id) fail('PROPERTY_ID_REQUIRED_FOR_STABLE_UPSERT');\n  return id;\n}\n\nfunction bookingUpsertKey({ stablePropertyKey, canonicalUrl, propertyId, checkIn, checkOut, currency }) {\n  const identity = bookingStableIdentity({ stablePropertyKey, canonicalUrl, propertyId });\n  if (!DATE_RE.test(String(checkIn ?? '')) || !DATE_RE.test(String(checkOut ?? ''))) fail('DATES_REQUIRED_FOR_STABLE_UPSERT');\n  const normalizedCurrency = upper(currency);\n  if (!CURRENCIES.has(normalizedCurrency)) fail('CURRENCY_REQUIRED_FOR_STABLE_UPSERT');\n  return `${identity}|${checkIn}|${checkOut}|${normalizedCurrency}`;\n}\n\nfunction scalar(value) {\n  if (value === null || value === undefined) return '';\n  if (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean') return value;\n  return JSON.stringify(value);\n}\n\nfunction prepareSheetRow(row, config, context) {\n  const search = row.searchParameters;\n  if (!search || typeof search !== 'object'\n    || String(search.checkIn ?? '') !== config.checkIn\n    || String(search.checkOut ?? '') !== config.checkOut\n    || upper(search.currency) !== config.currency) {\n    fail('PROPERTY_SEARCH_CONTEXT_MISMATCH');\n  }\n  const upsertKey = bookingUpsertKey({\n    stablePropertyKey: row.stablePropertyKey,\n    canonicalUrl: row.canonicalUrl,\n    propertyId: row.propertyId,\n    checkIn: config.checkIn,\n    checkOut: config.checkOut,\n    currency: config.currency,\n  });\n  return {\n    upsertKey,\n    stablePropertyKey: scalar(row.stablePropertyKey || bookingStableIdentity(row)),\n    propertyId: scalar(row.propertyId),\n    name: scalar(row.name),\n    checkIn: config.checkIn,\n    checkOut: config.checkOut,\n    requestedCurrency: config.currency,\n    resultCurrency: scalar(row.priceCurrency),\n    stayTotalAmount: scalar(row.stayTotalAmount),\n    stayNights: scalar(row.stayNights),\n    nightlyAmount: scalar(row.nightlyAmount),\n    guestReviewScore: scalar(row.guestReviewScore),\n    guestReviewCount: scalar(row.guestReviewCount),\n    stars: scalar(row.stars),\n    availabilityStatus: scalar(row.availabilityStatus),\n    locationText: scalar(row.locationText),\n    canonicalUrl: scalar(row.canonicalUrl),\n    priceBasis: scalar(row.priceBasis),\n    checkoutVerified: scalar(row.checkoutVerified),\n    collectedAt: scalar(row.collectedAt),\n    coverageState: context.coverageState,\n    workflowState: context.workflowState,\n    exactActorChargeUsd: context.buyerChargeUsd,\n    actorRunId: context.runId,\n  };\n}\n\nfunction reconcileBookingHotelsRun({ run = {}, summary = {}, rows = [], config = {} }) {\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  const datasetUrl = run.defaultDatasetId\n    ? `https://console.apify.com/storage/datasets/${run.defaultDatasetId}`\n    : runUrl;\n  const buyerChargeUsd = Number(summary?.ppe?.actualBuyerChargeUsd);\n  const base = {\n    run, summary, runId, runUrl, datasetUrl, buyerChargeUsd,\n    baselineState: 'NOT_APPLICABLE_STATELESS_SNAPSHOT',\n    unchangedState: 'NOT_CLAIMED_GOOGLE_SHEETS_UPSERT_ONLY',\n  };\n  if (!Number.isFinite(buyerChargeUsd) || buyerChargeUsd < 0) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'AUTHORITATIVE_BUYER_CHARGE_MISSING', shouldWrite: false, sheetRows: [] };\n  }\n  const zeroCapacityGuard = Number(summary.targetCount) === 0\n    && Number(summary.outputRows) === 0\n    && upper(summary.coverageState) === 'CAPPED'\n    && summary.ppe?.deliveryEventLimitReached === true;\n  if (!zeroCapacityGuard && Number(summary.targetCount) !== Number(config.targetCount)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'TARGET_RECONCILIATION_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n  if (rows.some((row) => row?.error)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DATASET_READ_ERROR', shouldWrite: false, sheetRows: [] };\n  }\n  const normalized = rows.filter((row) => row && typeof row === 'object' && Object.keys(row).length && !row.error);\n  const properties = normalized.filter((row) => row.recordType === 'property');\n  const coverageRows = normalized.filter((row) => row.recordType === 'coverage');\n  if (coverageRows.length !== 1 || normalized.length !== Number(summary.expectedDatasetRows)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DATASET_OR_COVERAGE_COUNT_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n  if (properties.length !== Number(summary.outputRows)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'PROPERTY_ROW_COUNT_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n\n  const coverageRecord = coverageRows[0];\n  if (Number(coverageRecord.targetCount) !== Number(summary.targetCount)\n    || upper(coverageRecord.coverageState) !== upper(summary.coverageState)\n    || upper(coverageRecord.coverageStatus) !== upper(summary.coverageStatus)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'OUTPUT_DATASET_COVERAGE_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n\n  if (zeroCapacityGuard) {\n    return {\n      ...base,\n      state: 'CAPPED',\n      reason: 'PPE_ALLOWANCE_ZERO_SOURCE_NOT_STARTED',\n      coverageState: 'CAPPED',\n      shouldWrite: false,\n      sheetRows: [],\n    };\n  }\n\n  const coverageState = upper(summary.coverageState || coverageRecord.coverageState);\n  const status = upper(summary.status || summary.coverageStatus);\n  if (['FAILED', 'BLOCKED'].includes(coverageState) || status === 'FAILED') {\n    return { ...base, state: 'FAILED', reason: 'SOURCE_FAILED_OR_BLOCKED', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (coverageState === 'PARTIAL' || status === 'PARTIAL') {\n    return { ...base, state: 'PARTIAL', reason: 'SOURCE_COVERAGE_PARTIAL', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  const workflowState = coverageState === 'CAPPED' || summary.ppe?.deliveryEventLimitReached === true\n    ? 'CAPPED'\n    : 'READY_TO_WRITE';\n\n  const seen = new Set();\n  const sheetRows = [];\n  try {\n    for (const property of properties) {\n      const row = prepareSheetRow(property, config, { coverageState, workflowState, buyerChargeUsd, runId });\n      if (seen.has(row.upsertKey)) continue;\n      seen.add(row.upsertKey);\n      sheetRows.push(row);\n    }\n  } catch (error) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: error.message, coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (sheetRows.length !== properties.length) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DUPLICATE_UPSERT_KEYS_IN_DATASET', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  const allEmpty = coverageState === 'EMPTY_CONFIRMED';\n  if (!sheetRows.length && allEmpty) {\n    return { ...base, state: 'EMPTY_VERIFIED', reason: 'SOURCE_VERIFIED_EMPTY', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (!sheetRows.length) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'ZERO_ROWS_WITHOUT_VERIFIED_EMPTY', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  return {\n    ...base,\n    state: workflowState,\n    reason: workflowState === 'CAPPED' ? 'BOUNDED_SHORTLIST_WRITTEN_WITH_CAP_DISCLOSED' : 'VERIFIED_SHORTLIST_READY',\n    coverageState,\n    shouldWrite: true,\n    sheetRows,\n  };\n}\n\nfunction renderBookingReceipt(result = {}, writtenRows = null) {\n  const labels = {\n    READY_TO_WRITE: 'Booking.com shortlist verified',\n    CAPPED: 'Booking.com bounded shortlist verified',\n    EMPTY_VERIFIED: 'Booking.com search verified empty',\n    PARTIAL: 'Booking.com search partial — no Sheet write',\n    FAILED: 'Booking.com source failed — no Sheet write',\n    ACTOR_FAILED_OR_ABORTED: 'Booking Hotels Actor failed or stopped',\n    WORKFLOW_NODE_FAILED: 'Workflow could not verify Booking.com output',\n  };\n  return {\n    state: result.state,\n    label: labels[result.state] ?? 'Booking.com shortlist result',\n    reason: result.reason ?? null,\n    rowsPrepared: Array.isArray(result.sheetRows) ? result.sheetRows.length : 0,\n    rowsWritten: Number.isInteger(writtenRows) ? writtenRows : null,\n    exactActorChargeUsd: Number.isFinite(Number(result.buyerChargeUsd)) ? Number(result.buyerChargeUsd) : null,\n    coverageState: result.coverageState ?? null,\n    baselineState: result.baselineState ?? 'NOT_APPLICABLE_STATELESS_SNAPSHOT',\n    unchangedState: result.unchangedState ?? 'NOT_CLAIMED_GOOGLE_SHEETS_UPSERT_ONLY',\n    runId: result.runId ?? null,\n    runUrl: result.runUrl ?? 'https://console.apify.com/actors/runs',\n    datasetUrl: result.datasetUrl ?? result.runUrl ?? 'https://console.apify.com/actors/runs',\n  };\n}\n\nconst BOOKING_HOTELS_WORKFLOW_LIMITS = Object.freeze({ maxPropertyRows: MAX_PROPERTY_ROWS, maxDatasetRows: MAX_DATASET_ROWS, pageSize: PAGE_SIZE });\n\n\nconst result = $input.first().json; return result.sheetRows.map((row) => ({ json: row }));"
      },
      "id": "41657c7cac136abfbb7d2784",
      "name": "Expand stable upsert rows",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3488,
        -80
      ]
    },
    {
      "parameters": {
        "resource": "sheet",
        "operation": "appendOrUpdate",
        "documentId": {
          "__rl": true,
          "mode": "id",
          "value": "={{ $('Fail-closed preflight').first().json.spreadsheetId }}"
        },
        "sheetName": {
          "__rl": true,
          "mode": "name",
          "value": "={{ $('Fail-closed preflight').first().json.sheetName }}"
        },
        "columns": {
          "mappingMode": "autoMapInputData",
          "value": {},
          "matchingColumns": [
            "upsertKey"
          ],
          "schema": [
            {
              "id": "upsertKey",
              "displayName": "upsertKey",
              "required": false,
              "defaultMatch": true,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true,
              "removed": false
            },
            {
              "id": "stablePropertyKey",
              "displayName": "stablePropertyKey",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "propertyId",
              "displayName": "propertyId",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "name",
              "displayName": "name",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "checkIn",
              "displayName": "checkIn",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "checkOut",
              "displayName": "checkOut",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "requestedCurrency",
              "displayName": "requestedCurrency",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "resultCurrency",
              "displayName": "resultCurrency",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "stayTotalAmount",
              "displayName": "stayTotalAmount",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "number",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "stayNights",
              "displayName": "stayNights",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "number",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "nightlyAmount",
              "displayName": "nightlyAmount",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "number",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "guestReviewScore",
              "displayName": "guestReviewScore",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "number",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "guestReviewCount",
              "displayName": "guestReviewCount",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "number",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "stars",
              "displayName": "stars",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "number",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "availabilityStatus",
              "displayName": "availabilityStatus",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "locationText",
              "displayName": "locationText",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "canonicalUrl",
              "displayName": "canonicalUrl",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "priceBasis",
              "displayName": "priceBasis",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "checkoutVerified",
              "displayName": "checkoutVerified",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "collectedAt",
              "displayName": "collectedAt",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "coverageState",
              "displayName": "coverageState",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "workflowState",
              "displayName": "workflowState",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "exactActorChargeUsd",
              "displayName": "exactActorChargeUsd",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "number",
              "canBeUsedToMatch": false,
              "removed": false
            },
            {
              "id": "actorRunId",
              "displayName": "actorRunId",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": false,
              "removed": false
            }
          ]
        },
        "options": {
          "cellFormat": "USER_ENTERED",
          "handlingExtraData": "insertInNewColumn",
          "useAppend": true
        }
      },
      "id": "0a32af782787282cb170ce96",
      "name": "Upsert dated shortlist in Google Sheets",
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.7,
      "position": [
        4040,
        -80
      ],
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 2000
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// runtimeSourceSha256=a1f7a6f4e65bed3b976cad220ac0fc356239532e6427be18a40d9e7eb1f3ea5c\nconst CURRENCIES = new Set(['EUR', 'USD', 'GBP', 'CHF', 'CAD', 'AUD', 'JPY', 'PLN', 'AED', 'INR', 'TRY']);\nconst LANGUAGES = new Set(['en-gb', 'en-us', 'de', 'fr', 'es', 'it', 'nl', 'pt-pt', 'pl', 'ja', 'ko', 'zh-cn']);\nconst SORTS = new Set(['popularity', 'price_low', 'price_high', 'stars', 'distance', 'review_score']);\nconst MAX_PROPERTY_ROWS = 1000;\nconst MAX_DATASET_ROWS = 1001;\nconst PAGE_SIZE = 1000;\nconst DATE_RE = /^\\d{4}-\\d{2}-\\d{2}$/;\n\nfunction fail(code) {\n  throw new Error(code);\n}\n\nfunction upper(value) {\n  return String(value ?? '').trim().toUpperCase();\n}\n\nfunction boundedInteger(value, code, minimum, maximum) {\n  const parsed = Number(value);\n  if (!Number.isInteger(parsed) || parsed < minimum || parsed > maximum) fail(code);\n  return parsed;\n}\n\nfunction positiveFinite(value, code, minimum, maximum) {\n  const parsed = Number(value);\n  if (!Number.isFinite(parsed) || parsed < minimum || parsed > maximum) fail(code);\n  return parsed;\n}\n\nfunction isoDate(value, placeholderCode, invalidCode) {\n  const text = String(value ?? '').trim();\n  if (!text || text.startsWith('REPLACE_WITH_')) fail(placeholderCode);\n  if (!DATE_RE.test(text) || Number.isNaN(Date.parse(`${text}T00:00:00Z`))) fail(invalidCode);\n  return text;\n}\n\nfunction preflightBookingHotelsConfig(raw = {}) {\n  const destination = String(raw.destination ?? '').trim();\n  if (!destination || destination === 'REPLACE_WITH_DESTINATION' || destination.length > 160) fail('DESTINATION_NOT_CONFIGURED');\n  const checkIn = isoDate(raw.checkIn, 'CHECK_IN_NOT_CONFIGURED', 'INVALID_CHECK_IN_DATE');\n  const checkOut = isoDate(raw.checkOut, 'CHECK_OUT_NOT_CONFIGURED', 'INVALID_CHECK_OUT_DATE');\n  if (Date.parse(`${checkOut}T00:00:00Z`) <= Date.parse(`${checkIn}T00:00:00Z`)) fail('CHECK_OUT_MUST_BE_AFTER_CHECK_IN');\n\n  const spreadsheetId = String(raw.spreadsheetId ?? '').trim();\n  const sheetName = String(raw.sheetName ?? '').trim();\n  if (!spreadsheetId || spreadsheetId === 'REPLACE_WITH_SPREADSHEET_ID') fail('SPREADSHEET_ID_NOT_CONFIGURED');\n  if (!/^[a-zA-Z0-9-_]{20,}$/.test(spreadsheetId)) fail('INVALID_SPREADSHEET_ID');\n  if (!sheetName || sheetName === 'REPLACE_WITH_SHEET_NAME' || sheetName.length > 100) fail('SHEET_NAME_NOT_CONFIGURED');\n\n  const adults = boundedInteger(raw.adults, 'INVALID_ADULTS_1_TO_30', 1, 30);\n  const rooms = boundedInteger(raw.rooms, 'INVALID_ROOMS_1_TO_30', 1, 30);\n  const maxProperties = boundedInteger(raw.maxProperties, 'INVALID_MAX_PROPERTIES_1_TO_1000', 1, MAX_PROPERTY_ROWS);\n  const maxPagesPerTarget = boundedInteger(raw.maxPagesPerTarget, 'INVALID_MAX_PAGES_1_TO_40', 1, 40);\n  const maxRequests = boundedInteger(raw.maxRequests, 'INVALID_MAX_REQUESTS_1_TO_250', 1, 250);\n  const maxRuntimeSecs = boundedInteger(raw.maxRuntimeSecs, 'INVALID_MAX_RUNTIME_30_TO_3600', 30, 3600);\n\n  const currency = upper(raw.currency || 'EUR');\n  const language = String(raw.language ?? 'en-gb').trim().toLowerCase();\n  const sortBy = String(raw.sortBy ?? 'popularity').trim();\n  if (!CURRENCIES.has(currency)) fail('INVALID_CURRENCY');\n  if (!LANGUAGES.has(language)) fail('INVALID_LANGUAGE');\n  if (!SORTS.has(sortBy)) fail('INVALID_SORT_ORDER');\n  const maxCostPerRunUsd = positiveFinite(raw.maxCostPerRunUsd, 'INVALID_MAX_CHARGE_CAP', 0.02139, 5);\n\n  return {\n    destination,\n    checkIn,\n    checkOut,\n    currency,\n    spreadsheetId,\n    sheetName,\n    targetCount: 1,\n    maxProperties,\n    maxCostPerRunUsd,\n    actorInput: {\n      destination,\n      checkIn,\n      checkOut,\n      adults,\n      rooms,\n      children: 0,\n      childrenAges: [],\n      flexibleDays: '0',\n      maxProperties,\n      mode: 'fast',\n      sortBy,\n      currency,\n      language,\n      maxPagesPerTarget,\n      maxRequests,\n      maxRuntimeSecs,\n      maxCostUsd: 0.25,\n      maxBuyerChargeUsd: maxCostPerRunUsd,\n      requestTimeoutSecs: 35,\n      maxRetries: 1,\n    },\n  };\n}\n\nfunction classifyBookingTerminalRun(run = {}, expectedBuildId = '') {\n  const status = upper(run.status);\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  if (run.error || !status) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'APIFY_RUN_NODE_ERROR' };\n  }\n  if (status !== 'SUCCEEDED') {\n    return { ok: false, state: 'ACTOR_FAILED_OR_ABORTED', run, runId, runUrl, reason: `ACTOR_STATUS_${status}` };\n  }\n  if (!expectedBuildId || String(run.buildId ?? '') !== String(expectedBuildId)) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'RUN_BUILD_ID_MISMATCH' };\n  }\n  if (!run.defaultKeyValueStoreId || !run.defaultDatasetId) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'RUN_STORAGE_IDS_MISSING' };\n  }\n  return { ok: true, state: 'RUN_SUCCEEDED', run, runId, runUrl };\n}\n\nfunction exactBookingBuyerCharge(billing) {\n  if (billing?.billingMode !== 'pay_per_event') fail('PPE_PRICING_NOT_ACTIVE');\n  const prices = billing.effectiveEventPricesUsd;\n  const counts = billing.counts;\n  if (!prices || typeof prices !== 'object' || !counts || typeof counts !== 'object') {\n    fail('AUTHORITATIVE_PPE_ACCOUNTING_MISSING');\n  }\n  const chargedCounts = {};\n  let total = 0;\n  for (const [eventName, eventCounts] of Object.entries(counts)) {\n    const charged = Number(eventCounts?.charged ?? 0);\n    if (!Number.isInteger(charged) || charged < 0) fail('INVALID_CHARGED_EVENT_COUNT');\n    chargedCounts[eventName] = charged;\n    if (charged === 0) continue;\n    const price = Number(prices[eventName]);\n    if (!Number.isFinite(price) || price < 0) fail('INVALID_EFFECTIVE_EVENT_PRICE');\n    total += charged * price;\n  }\n  return {\n    actualBuyerChargeUsd: Number(total.toFixed(12)),\n    chargedCounts,\n    effectivePricesUsd: { ...prices },\n  };\n}\n\nfunction parseBookingOutputRecord(record = {}, run = {}) {\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  const identity = { run, runId, runUrl };\n  const recordIsObject = record && typeof record === 'object' && !Array.isArray(record);\n  const data = recordIsObject && !record.error\n    ? (Object.prototype.hasOwnProperty.call(record, 'data') ? record.data : record)\n    : null;\n  if (!data || typeof data !== 'object' || Array.isArray(data) || data.error) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_SUMMARY_MISSING_OR_INVALID' };\n  }\n  let ppe;\n  try { ppe = exactBookingBuyerCharge(data.billing); } catch (error) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: error.message };\n  }\n  const outputRows = Number(data.propertyRows);\n  const targetCount = Number(data.targetCount);\n  if (!Number.isInteger(outputRows) || outputRows < 0 || outputRows > MAX_PROPERTY_ROWS) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_PROPERTY_ROW_COUNT_INVALID' };\n  }\n  const zeroCapacityGuard = targetCount === 0\n    && outputRows === 0\n    && upper(data.coverageState) === 'CAPPED'\n    && upper(data.coverageStatus) === 'CAPPED'\n    && data.stoppedByBillingGuard === true\n    && Array.isArray(data.perTarget)\n    && data.perTarget.length === 0;\n  if (!Number.isInteger(targetCount) || targetCount < 0 || !Array.isArray(data.perTarget)\n    || data.perTarget.length !== targetCount || (targetCount === 0 && !zeroCapacityGuard)) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_TARGET_CONTRACT_MISSING' };\n  }\n  if (Number(ppe.chargedCounts.fast_property_result ?? 0) !== outputRows) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'PPE_PROPERTY_DELIVERY_COUNT_MISMATCH' };\n  }\n  return {\n    ok: true,\n    state: 'SUMMARY_READY',\n    ...identity,\n    buyerChargeUsd: ppe.actualBuyerChargeUsd,\n    summary: {\n      schemaVersion: 2,\n      sourceRecordKey: 'OUTPUT',\n      status: upper(data.status),\n      coverageState: upper(data.coverageState),\n      coverageStatus: upper(data.coverageStatus),\n      outputRows,\n      expectedDatasetRows: outputRows + 1,\n      targetCount,\n      perTarget: data.perTarget,\n      warnings: Array.isArray(data.warnings) ? data.warnings.map(String) : [],\n      ppe: {\n        ...ppe,\n        deliveryEventLimitReached: data.billing?.eventLimitReached === true || data.stoppedByBillingGuard === true,\n      },\n    },\n  };\n}\n\nfunction planBookingDatasetPages(summary, pageSize = PAGE_SIZE) {\n  const total = boundedInteger(summary?.expectedDatasetRows, 'INVALID_SUMMARY_DATASET_ROWS', 1, MAX_DATASET_ROWS);\n  const size = boundedInteger(pageSize, 'INVALID_DATASET_PAGE_SIZE', 1, PAGE_SIZE);\n  return Array.from({ length: Math.ceil(total / size) }, (_, index) => ({\n    offset: index * size,\n    limit: Math.min(size, total - (index * size)),\n    expectedRows: total,\n  }));\n}\n\nfunction normalizedBookingPropertyPath(canonicalUrl) {\n  try {\n    const url = new URL(String(canonicalUrl ?? '').trim());\n    if (!/(^|\\.)booking\\.com$/i.test(url.hostname) || !/^\\/hotel\\//i.test(url.pathname)) return null;\n    return url.pathname.toLowerCase()\n      .replace(/\\/{2,}/g, '/')\n      .replace(/\\.[a-z]{2}(?:-[a-z]{2})?\\.html$/i, '.html');\n  } catch {\n    return null;\n  }\n}\n\nfunction bookingStableIdentity({ stablePropertyKey, canonicalUrl, propertyId }) {\n  const canonicalPath = normalizedBookingPropertyPath(canonicalUrl);\n  if (canonicalPath) return `booking_path:${canonicalPath}`;\n  const actorStableKey = String(stablePropertyKey ?? '').trim();\n  if (actorStableKey) return actorStableKey;\n  const id = String(propertyId ?? '').trim();\n  if (!id) fail('PROPERTY_ID_REQUIRED_FOR_STABLE_UPSERT');\n  return id;\n}\n\nfunction bookingUpsertKey({ stablePropertyKey, canonicalUrl, propertyId, checkIn, checkOut, currency }) {\n  const identity = bookingStableIdentity({ stablePropertyKey, canonicalUrl, propertyId });\n  if (!DATE_RE.test(String(checkIn ?? '')) || !DATE_RE.test(String(checkOut ?? ''))) fail('DATES_REQUIRED_FOR_STABLE_UPSERT');\n  const normalizedCurrency = upper(currency);\n  if (!CURRENCIES.has(normalizedCurrency)) fail('CURRENCY_REQUIRED_FOR_STABLE_UPSERT');\n  return `${identity}|${checkIn}|${checkOut}|${normalizedCurrency}`;\n}\n\nfunction scalar(value) {\n  if (value === null || value === undefined) return '';\n  if (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean') return value;\n  return JSON.stringify(value);\n}\n\nfunction prepareSheetRow(row, config, context) {\n  const search = row.searchParameters;\n  if (!search || typeof search !== 'object'\n    || String(search.checkIn ?? '') !== config.checkIn\n    || String(search.checkOut ?? '') !== config.checkOut\n    || upper(search.currency) !== config.currency) {\n    fail('PROPERTY_SEARCH_CONTEXT_MISMATCH');\n  }\n  const upsertKey = bookingUpsertKey({\n    stablePropertyKey: row.stablePropertyKey,\n    canonicalUrl: row.canonicalUrl,\n    propertyId: row.propertyId,\n    checkIn: config.checkIn,\n    checkOut: config.checkOut,\n    currency: config.currency,\n  });\n  return {\n    upsertKey,\n    stablePropertyKey: scalar(row.stablePropertyKey || bookingStableIdentity(row)),\n    propertyId: scalar(row.propertyId),\n    name: scalar(row.name),\n    checkIn: config.checkIn,\n    checkOut: config.checkOut,\n    requestedCurrency: config.currency,\n    resultCurrency: scalar(row.priceCurrency),\n    stayTotalAmount: scalar(row.stayTotalAmount),\n    stayNights: scalar(row.stayNights),\n    nightlyAmount: scalar(row.nightlyAmount),\n    guestReviewScore: scalar(row.guestReviewScore),\n    guestReviewCount: scalar(row.guestReviewCount),\n    stars: scalar(row.stars),\n    availabilityStatus: scalar(row.availabilityStatus),\n    locationText: scalar(row.locationText),\n    canonicalUrl: scalar(row.canonicalUrl),\n    priceBasis: scalar(row.priceBasis),\n    checkoutVerified: scalar(row.checkoutVerified),\n    collectedAt: scalar(row.collectedAt),\n    coverageState: context.coverageState,\n    workflowState: context.workflowState,\n    exactActorChargeUsd: context.buyerChargeUsd,\n    actorRunId: context.runId,\n  };\n}\n\nfunction reconcileBookingHotelsRun({ run = {}, summary = {}, rows = [], config = {} }) {\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  const datasetUrl = run.defaultDatasetId\n    ? `https://console.apify.com/storage/datasets/${run.defaultDatasetId}`\n    : runUrl;\n  const buyerChargeUsd = Number(summary?.ppe?.actualBuyerChargeUsd);\n  const base = {\n    run, summary, runId, runUrl, datasetUrl, buyerChargeUsd,\n    baselineState: 'NOT_APPLICABLE_STATELESS_SNAPSHOT',\n    unchangedState: 'NOT_CLAIMED_GOOGLE_SHEETS_UPSERT_ONLY',\n  };\n  if (!Number.isFinite(buyerChargeUsd) || buyerChargeUsd < 0) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'AUTHORITATIVE_BUYER_CHARGE_MISSING', shouldWrite: false, sheetRows: [] };\n  }\n  const zeroCapacityGuard = Number(summary.targetCount) === 0\n    && Number(summary.outputRows) === 0\n    && upper(summary.coverageState) === 'CAPPED'\n    && summary.ppe?.deliveryEventLimitReached === true;\n  if (!zeroCapacityGuard && Number(summary.targetCount) !== Number(config.targetCount)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'TARGET_RECONCILIATION_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n  if (rows.some((row) => row?.error)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DATASET_READ_ERROR', shouldWrite: false, sheetRows: [] };\n  }\n  const normalized = rows.filter((row) => row && typeof row === 'object' && Object.keys(row).length && !row.error);\n  const properties = normalized.filter((row) => row.recordType === 'property');\n  const coverageRows = normalized.filter((row) => row.recordType === 'coverage');\n  if (coverageRows.length !== 1 || normalized.length !== Number(summary.expectedDatasetRows)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DATASET_OR_COVERAGE_COUNT_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n  if (properties.length !== Number(summary.outputRows)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'PROPERTY_ROW_COUNT_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n\n  const coverageRecord = coverageRows[0];\n  if (Number(coverageRecord.targetCount) !== Number(summary.targetCount)\n    || upper(coverageRecord.coverageState) !== upper(summary.coverageState)\n    || upper(coverageRecord.coverageStatus) !== upper(summary.coverageStatus)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'OUTPUT_DATASET_COVERAGE_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n\n  if (zeroCapacityGuard) {\n    return {\n      ...base,\n      state: 'CAPPED',\n      reason: 'PPE_ALLOWANCE_ZERO_SOURCE_NOT_STARTED',\n      coverageState: 'CAPPED',\n      shouldWrite: false,\n      sheetRows: [],\n    };\n  }\n\n  const coverageState = upper(summary.coverageState || coverageRecord.coverageState);\n  const status = upper(summary.status || summary.coverageStatus);\n  if (['FAILED', 'BLOCKED'].includes(coverageState) || status === 'FAILED') {\n    return { ...base, state: 'FAILED', reason: 'SOURCE_FAILED_OR_BLOCKED', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (coverageState === 'PARTIAL' || status === 'PARTIAL') {\n    return { ...base, state: 'PARTIAL', reason: 'SOURCE_COVERAGE_PARTIAL', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  const workflowState = coverageState === 'CAPPED' || summary.ppe?.deliveryEventLimitReached === true\n    ? 'CAPPED'\n    : 'READY_TO_WRITE';\n\n  const seen = new Set();\n  const sheetRows = [];\n  try {\n    for (const property of properties) {\n      const row = prepareSheetRow(property, config, { coverageState, workflowState, buyerChargeUsd, runId });\n      if (seen.has(row.upsertKey)) continue;\n      seen.add(row.upsertKey);\n      sheetRows.push(row);\n    }\n  } catch (error) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: error.message, coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (sheetRows.length !== properties.length) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DUPLICATE_UPSERT_KEYS_IN_DATASET', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  const allEmpty = coverageState === 'EMPTY_CONFIRMED';\n  if (!sheetRows.length && allEmpty) {\n    return { ...base, state: 'EMPTY_VERIFIED', reason: 'SOURCE_VERIFIED_EMPTY', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (!sheetRows.length) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'ZERO_ROWS_WITHOUT_VERIFIED_EMPTY', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  return {\n    ...base,\n    state: workflowState,\n    reason: workflowState === 'CAPPED' ? 'BOUNDED_SHORTLIST_WRITTEN_WITH_CAP_DISCLOSED' : 'VERIFIED_SHORTLIST_READY',\n    coverageState,\n    shouldWrite: true,\n    sheetRows,\n  };\n}\n\nfunction renderBookingReceipt(result = {}, writtenRows = null) {\n  const labels = {\n    READY_TO_WRITE: 'Booking.com shortlist verified',\n    CAPPED: 'Booking.com bounded shortlist verified',\n    EMPTY_VERIFIED: 'Booking.com search verified empty',\n    PARTIAL: 'Booking.com search partial — no Sheet write',\n    FAILED: 'Booking.com source failed — no Sheet write',\n    ACTOR_FAILED_OR_ABORTED: 'Booking Hotels Actor failed or stopped',\n    WORKFLOW_NODE_FAILED: 'Workflow could not verify Booking.com output',\n  };\n  return {\n    state: result.state,\n    label: labels[result.state] ?? 'Booking.com shortlist result',\n    reason: result.reason ?? null,\n    rowsPrepared: Array.isArray(result.sheetRows) ? result.sheetRows.length : 0,\n    rowsWritten: Number.isInteger(writtenRows) ? writtenRows : null,\n    exactActorChargeUsd: Number.isFinite(Number(result.buyerChargeUsd)) ? Number(result.buyerChargeUsd) : null,\n    coverageState: result.coverageState ?? null,\n    baselineState: result.baselineState ?? 'NOT_APPLICABLE_STATELESS_SNAPSHOT',\n    unchangedState: result.unchangedState ?? 'NOT_CLAIMED_GOOGLE_SHEETS_UPSERT_ONLY',\n    runId: result.runId ?? null,\n    runUrl: result.runUrl ?? 'https://console.apify.com/actors/runs',\n    datasetUrl: result.datasetUrl ?? result.runUrl ?? 'https://console.apify.com/actors/runs',\n  };\n}\n\nconst BOOKING_HOTELS_WORKFLOW_LIMITS = Object.freeze({ maxPropertyRows: MAX_PROPERTY_ROWS, maxDatasetRows: MAX_DATASET_ROWS, pageSize: PAGE_SIZE });\n\n\nconst result = $('Reconcile rows coverage and exact charge').first().json;\nreturn [{ json: renderBookingReceipt(result, $input.all().length) }];"
      },
      "id": "d707e7340f4ebabcc3717557",
      "name": "Return successful Sheet receipt",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        4280,
        -80
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// runtimeSourceSha256=a1f7a6f4e65bed3b976cad220ac0fc356239532e6427be18a40d9e7eb1f3ea5c\nconst CURRENCIES = new Set(['EUR', 'USD', 'GBP', 'CHF', 'CAD', 'AUD', 'JPY', 'PLN', 'AED', 'INR', 'TRY']);\nconst LANGUAGES = new Set(['en-gb', 'en-us', 'de', 'fr', 'es', 'it', 'nl', 'pt-pt', 'pl', 'ja', 'ko', 'zh-cn']);\nconst SORTS = new Set(['popularity', 'price_low', 'price_high', 'stars', 'distance', 'review_score']);\nconst MAX_PROPERTY_ROWS = 1000;\nconst MAX_DATASET_ROWS = 1001;\nconst PAGE_SIZE = 1000;\nconst DATE_RE = /^\\d{4}-\\d{2}-\\d{2}$/;\n\nfunction fail(code) {\n  throw new Error(code);\n}\n\nfunction upper(value) {\n  return String(value ?? '').trim().toUpperCase();\n}\n\nfunction boundedInteger(value, code, minimum, maximum) {\n  const parsed = Number(value);\n  if (!Number.isInteger(parsed) || parsed < minimum || parsed > maximum) fail(code);\n  return parsed;\n}\n\nfunction positiveFinite(value, code, minimum, maximum) {\n  const parsed = Number(value);\n  if (!Number.isFinite(parsed) || parsed < minimum || parsed > maximum) fail(code);\n  return parsed;\n}\n\nfunction isoDate(value, placeholderCode, invalidCode) {\n  const text = String(value ?? '').trim();\n  if (!text || text.startsWith('REPLACE_WITH_')) fail(placeholderCode);\n  if (!DATE_RE.test(text) || Number.isNaN(Date.parse(`${text}T00:00:00Z`))) fail(invalidCode);\n  return text;\n}\n\nfunction preflightBookingHotelsConfig(raw = {}) {\n  const destination = String(raw.destination ?? '').trim();\n  if (!destination || destination === 'REPLACE_WITH_DESTINATION' || destination.length > 160) fail('DESTINATION_NOT_CONFIGURED');\n  const checkIn = isoDate(raw.checkIn, 'CHECK_IN_NOT_CONFIGURED', 'INVALID_CHECK_IN_DATE');\n  const checkOut = isoDate(raw.checkOut, 'CHECK_OUT_NOT_CONFIGURED', 'INVALID_CHECK_OUT_DATE');\n  if (Date.parse(`${checkOut}T00:00:00Z`) <= Date.parse(`${checkIn}T00:00:00Z`)) fail('CHECK_OUT_MUST_BE_AFTER_CHECK_IN');\n\n  const spreadsheetId = String(raw.spreadsheetId ?? '').trim();\n  const sheetName = String(raw.sheetName ?? '').trim();\n  if (!spreadsheetId || spreadsheetId === 'REPLACE_WITH_SPREADSHEET_ID') fail('SPREADSHEET_ID_NOT_CONFIGURED');\n  if (!/^[a-zA-Z0-9-_]{20,}$/.test(spreadsheetId)) fail('INVALID_SPREADSHEET_ID');\n  if (!sheetName || sheetName === 'REPLACE_WITH_SHEET_NAME' || sheetName.length > 100) fail('SHEET_NAME_NOT_CONFIGURED');\n\n  const adults = boundedInteger(raw.adults, 'INVALID_ADULTS_1_TO_30', 1, 30);\n  const rooms = boundedInteger(raw.rooms, 'INVALID_ROOMS_1_TO_30', 1, 30);\n  const maxProperties = boundedInteger(raw.maxProperties, 'INVALID_MAX_PROPERTIES_1_TO_1000', 1, MAX_PROPERTY_ROWS);\n  const maxPagesPerTarget = boundedInteger(raw.maxPagesPerTarget, 'INVALID_MAX_PAGES_1_TO_40', 1, 40);\n  const maxRequests = boundedInteger(raw.maxRequests, 'INVALID_MAX_REQUESTS_1_TO_250', 1, 250);\n  const maxRuntimeSecs = boundedInteger(raw.maxRuntimeSecs, 'INVALID_MAX_RUNTIME_30_TO_3600', 30, 3600);\n\n  const currency = upper(raw.currency || 'EUR');\n  const language = String(raw.language ?? 'en-gb').trim().toLowerCase();\n  const sortBy = String(raw.sortBy ?? 'popularity').trim();\n  if (!CURRENCIES.has(currency)) fail('INVALID_CURRENCY');\n  if (!LANGUAGES.has(language)) fail('INVALID_LANGUAGE');\n  if (!SORTS.has(sortBy)) fail('INVALID_SORT_ORDER');\n  const maxCostPerRunUsd = positiveFinite(raw.maxCostPerRunUsd, 'INVALID_MAX_CHARGE_CAP', 0.02139, 5);\n\n  return {\n    destination,\n    checkIn,\n    checkOut,\n    currency,\n    spreadsheetId,\n    sheetName,\n    targetCount: 1,\n    maxProperties,\n    maxCostPerRunUsd,\n    actorInput: {\n      destination,\n      checkIn,\n      checkOut,\n      adults,\n      rooms,\n      children: 0,\n      childrenAges: [],\n      flexibleDays: '0',\n      maxProperties,\n      mode: 'fast',\n      sortBy,\n      currency,\n      language,\n      maxPagesPerTarget,\n      maxRequests,\n      maxRuntimeSecs,\n      maxCostUsd: 0.25,\n      maxBuyerChargeUsd: maxCostPerRunUsd,\n      requestTimeoutSecs: 35,\n      maxRetries: 1,\n    },\n  };\n}\n\nfunction classifyBookingTerminalRun(run = {}, expectedBuildId = '') {\n  const status = upper(run.status);\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  if (run.error || !status) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'APIFY_RUN_NODE_ERROR' };\n  }\n  if (status !== 'SUCCEEDED') {\n    return { ok: false, state: 'ACTOR_FAILED_OR_ABORTED', run, runId, runUrl, reason: `ACTOR_STATUS_${status}` };\n  }\n  if (!expectedBuildId || String(run.buildId ?? '') !== String(expectedBuildId)) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'RUN_BUILD_ID_MISMATCH' };\n  }\n  if (!run.defaultKeyValueStoreId || !run.defaultDatasetId) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', run, runId, runUrl, reason: 'RUN_STORAGE_IDS_MISSING' };\n  }\n  return { ok: true, state: 'RUN_SUCCEEDED', run, runId, runUrl };\n}\n\nfunction exactBookingBuyerCharge(billing) {\n  if (billing?.billingMode !== 'pay_per_event') fail('PPE_PRICING_NOT_ACTIVE');\n  const prices = billing.effectiveEventPricesUsd;\n  const counts = billing.counts;\n  if (!prices || typeof prices !== 'object' || !counts || typeof counts !== 'object') {\n    fail('AUTHORITATIVE_PPE_ACCOUNTING_MISSING');\n  }\n  const chargedCounts = {};\n  let total = 0;\n  for (const [eventName, eventCounts] of Object.entries(counts)) {\n    const charged = Number(eventCounts?.charged ?? 0);\n    if (!Number.isInteger(charged) || charged < 0) fail('INVALID_CHARGED_EVENT_COUNT');\n    chargedCounts[eventName] = charged;\n    if (charged === 0) continue;\n    const price = Number(prices[eventName]);\n    if (!Number.isFinite(price) || price < 0) fail('INVALID_EFFECTIVE_EVENT_PRICE');\n    total += charged * price;\n  }\n  return {\n    actualBuyerChargeUsd: Number(total.toFixed(12)),\n    chargedCounts,\n    effectivePricesUsd: { ...prices },\n  };\n}\n\nfunction parseBookingOutputRecord(record = {}, run = {}) {\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  const identity = { run, runId, runUrl };\n  const recordIsObject = record && typeof record === 'object' && !Array.isArray(record);\n  const data = recordIsObject && !record.error\n    ? (Object.prototype.hasOwnProperty.call(record, 'data') ? record.data : record)\n    : null;\n  if (!data || typeof data !== 'object' || Array.isArray(data) || data.error) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_SUMMARY_MISSING_OR_INVALID' };\n  }\n  let ppe;\n  try { ppe = exactBookingBuyerCharge(data.billing); } catch (error) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: error.message };\n  }\n  const outputRows = Number(data.propertyRows);\n  const targetCount = Number(data.targetCount);\n  if (!Number.isInteger(outputRows) || outputRows < 0 || outputRows > MAX_PROPERTY_ROWS) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_PROPERTY_ROW_COUNT_INVALID' };\n  }\n  const zeroCapacityGuard = targetCount === 0\n    && outputRows === 0\n    && upper(data.coverageState) === 'CAPPED'\n    && upper(data.coverageStatus) === 'CAPPED'\n    && data.stoppedByBillingGuard === true\n    && Array.isArray(data.perTarget)\n    && data.perTarget.length === 0;\n  if (!Number.isInteger(targetCount) || targetCount < 0 || !Array.isArray(data.perTarget)\n    || data.perTarget.length !== targetCount || (targetCount === 0 && !zeroCapacityGuard)) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'OUTPUT_TARGET_CONTRACT_MISSING' };\n  }\n  if (Number(ppe.chargedCounts.fast_property_result ?? 0) !== outputRows) {\n    return { ok: false, state: 'WORKFLOW_NODE_FAILED', ...identity, reason: 'PPE_PROPERTY_DELIVERY_COUNT_MISMATCH' };\n  }\n  return {\n    ok: true,\n    state: 'SUMMARY_READY',\n    ...identity,\n    buyerChargeUsd: ppe.actualBuyerChargeUsd,\n    summary: {\n      schemaVersion: 2,\n      sourceRecordKey: 'OUTPUT',\n      status: upper(data.status),\n      coverageState: upper(data.coverageState),\n      coverageStatus: upper(data.coverageStatus),\n      outputRows,\n      expectedDatasetRows: outputRows + 1,\n      targetCount,\n      perTarget: data.perTarget,\n      warnings: Array.isArray(data.warnings) ? data.warnings.map(String) : [],\n      ppe: {\n        ...ppe,\n        deliveryEventLimitReached: data.billing?.eventLimitReached === true || data.stoppedByBillingGuard === true,\n      },\n    },\n  };\n}\n\nfunction planBookingDatasetPages(summary, pageSize = PAGE_SIZE) {\n  const total = boundedInteger(summary?.expectedDatasetRows, 'INVALID_SUMMARY_DATASET_ROWS', 1, MAX_DATASET_ROWS);\n  const size = boundedInteger(pageSize, 'INVALID_DATASET_PAGE_SIZE', 1, PAGE_SIZE);\n  return Array.from({ length: Math.ceil(total / size) }, (_, index) => ({\n    offset: index * size,\n    limit: Math.min(size, total - (index * size)),\n    expectedRows: total,\n  }));\n}\n\nfunction normalizedBookingPropertyPath(canonicalUrl) {\n  try {\n    const url = new URL(String(canonicalUrl ?? '').trim());\n    if (!/(^|\\.)booking\\.com$/i.test(url.hostname) || !/^\\/hotel\\//i.test(url.pathname)) return null;\n    return url.pathname.toLowerCase()\n      .replace(/\\/{2,}/g, '/')\n      .replace(/\\.[a-z]{2}(?:-[a-z]{2})?\\.html$/i, '.html');\n  } catch {\n    return null;\n  }\n}\n\nfunction bookingStableIdentity({ stablePropertyKey, canonicalUrl, propertyId }) {\n  const canonicalPath = normalizedBookingPropertyPath(canonicalUrl);\n  if (canonicalPath) return `booking_path:${canonicalPath}`;\n  const actorStableKey = String(stablePropertyKey ?? '').trim();\n  if (actorStableKey) return actorStableKey;\n  const id = String(propertyId ?? '').trim();\n  if (!id) fail('PROPERTY_ID_REQUIRED_FOR_STABLE_UPSERT');\n  return id;\n}\n\nfunction bookingUpsertKey({ stablePropertyKey, canonicalUrl, propertyId, checkIn, checkOut, currency }) {\n  const identity = bookingStableIdentity({ stablePropertyKey, canonicalUrl, propertyId });\n  if (!DATE_RE.test(String(checkIn ?? '')) || !DATE_RE.test(String(checkOut ?? ''))) fail('DATES_REQUIRED_FOR_STABLE_UPSERT');\n  const normalizedCurrency = upper(currency);\n  if (!CURRENCIES.has(normalizedCurrency)) fail('CURRENCY_REQUIRED_FOR_STABLE_UPSERT');\n  return `${identity}|${checkIn}|${checkOut}|${normalizedCurrency}`;\n}\n\nfunction scalar(value) {\n  if (value === null || value === undefined) return '';\n  if (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean') return value;\n  return JSON.stringify(value);\n}\n\nfunction prepareSheetRow(row, config, context) {\n  const search = row.searchParameters;\n  if (!search || typeof search !== 'object'\n    || String(search.checkIn ?? '') !== config.checkIn\n    || String(search.checkOut ?? '') !== config.checkOut\n    || upper(search.currency) !== config.currency) {\n    fail('PROPERTY_SEARCH_CONTEXT_MISMATCH');\n  }\n  const upsertKey = bookingUpsertKey({\n    stablePropertyKey: row.stablePropertyKey,\n    canonicalUrl: row.canonicalUrl,\n    propertyId: row.propertyId,\n    checkIn: config.checkIn,\n    checkOut: config.checkOut,\n    currency: config.currency,\n  });\n  return {\n    upsertKey,\n    stablePropertyKey: scalar(row.stablePropertyKey || bookingStableIdentity(row)),\n    propertyId: scalar(row.propertyId),\n    name: scalar(row.name),\n    checkIn: config.checkIn,\n    checkOut: config.checkOut,\n    requestedCurrency: config.currency,\n    resultCurrency: scalar(row.priceCurrency),\n    stayTotalAmount: scalar(row.stayTotalAmount),\n    stayNights: scalar(row.stayNights),\n    nightlyAmount: scalar(row.nightlyAmount),\n    guestReviewScore: scalar(row.guestReviewScore),\n    guestReviewCount: scalar(row.guestReviewCount),\n    stars: scalar(row.stars),\n    availabilityStatus: scalar(row.availabilityStatus),\n    locationText: scalar(row.locationText),\n    canonicalUrl: scalar(row.canonicalUrl),\n    priceBasis: scalar(row.priceBasis),\n    checkoutVerified: scalar(row.checkoutVerified),\n    collectedAt: scalar(row.collectedAt),\n    coverageState: context.coverageState,\n    workflowState: context.workflowState,\n    exactActorChargeUsd: context.buyerChargeUsd,\n    actorRunId: context.runId,\n  };\n}\n\nfunction reconcileBookingHotelsRun({ run = {}, summary = {}, rows = [], config = {} }) {\n  const runId = String(run.id ?? '');\n  const runUrl = runId ? `https://console.apify.com/actors/runs/${runId}` : 'https://console.apify.com/actors/runs';\n  const datasetUrl = run.defaultDatasetId\n    ? `https://console.apify.com/storage/datasets/${run.defaultDatasetId}`\n    : runUrl;\n  const buyerChargeUsd = Number(summary?.ppe?.actualBuyerChargeUsd);\n  const base = {\n    run, summary, runId, runUrl, datasetUrl, buyerChargeUsd,\n    baselineState: 'NOT_APPLICABLE_STATELESS_SNAPSHOT',\n    unchangedState: 'NOT_CLAIMED_GOOGLE_SHEETS_UPSERT_ONLY',\n  };\n  if (!Number.isFinite(buyerChargeUsd) || buyerChargeUsd < 0) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'AUTHORITATIVE_BUYER_CHARGE_MISSING', shouldWrite: false, sheetRows: [] };\n  }\n  const zeroCapacityGuard = Number(summary.targetCount) === 0\n    && Number(summary.outputRows) === 0\n    && upper(summary.coverageState) === 'CAPPED'\n    && summary.ppe?.deliveryEventLimitReached === true;\n  if (!zeroCapacityGuard && Number(summary.targetCount) !== Number(config.targetCount)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'TARGET_RECONCILIATION_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n  if (rows.some((row) => row?.error)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DATASET_READ_ERROR', shouldWrite: false, sheetRows: [] };\n  }\n  const normalized = rows.filter((row) => row && typeof row === 'object' && Object.keys(row).length && !row.error);\n  const properties = normalized.filter((row) => row.recordType === 'property');\n  const coverageRows = normalized.filter((row) => row.recordType === 'coverage');\n  if (coverageRows.length !== 1 || normalized.length !== Number(summary.expectedDatasetRows)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DATASET_OR_COVERAGE_COUNT_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n  if (properties.length !== Number(summary.outputRows)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'PROPERTY_ROW_COUNT_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n\n  const coverageRecord = coverageRows[0];\n  if (Number(coverageRecord.targetCount) !== Number(summary.targetCount)\n    || upper(coverageRecord.coverageState) !== upper(summary.coverageState)\n    || upper(coverageRecord.coverageStatus) !== upper(summary.coverageStatus)) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'OUTPUT_DATASET_COVERAGE_MISMATCH', shouldWrite: false, sheetRows: [] };\n  }\n\n  if (zeroCapacityGuard) {\n    return {\n      ...base,\n      state: 'CAPPED',\n      reason: 'PPE_ALLOWANCE_ZERO_SOURCE_NOT_STARTED',\n      coverageState: 'CAPPED',\n      shouldWrite: false,\n      sheetRows: [],\n    };\n  }\n\n  const coverageState = upper(summary.coverageState || coverageRecord.coverageState);\n  const status = upper(summary.status || summary.coverageStatus);\n  if (['FAILED', 'BLOCKED'].includes(coverageState) || status === 'FAILED') {\n    return { ...base, state: 'FAILED', reason: 'SOURCE_FAILED_OR_BLOCKED', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (coverageState === 'PARTIAL' || status === 'PARTIAL') {\n    return { ...base, state: 'PARTIAL', reason: 'SOURCE_COVERAGE_PARTIAL', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  const workflowState = coverageState === 'CAPPED' || summary.ppe?.deliveryEventLimitReached === true\n    ? 'CAPPED'\n    : 'READY_TO_WRITE';\n\n  const seen = new Set();\n  const sheetRows = [];\n  try {\n    for (const property of properties) {\n      const row = prepareSheetRow(property, config, { coverageState, workflowState, buyerChargeUsd, runId });\n      if (seen.has(row.upsertKey)) continue;\n      seen.add(row.upsertKey);\n      sheetRows.push(row);\n    }\n  } catch (error) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: error.message, coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (sheetRows.length !== properties.length) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'DUPLICATE_UPSERT_KEYS_IN_DATASET', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  const allEmpty = coverageState === 'EMPTY_CONFIRMED';\n  if (!sheetRows.length && allEmpty) {\n    return { ...base, state: 'EMPTY_VERIFIED', reason: 'SOURCE_VERIFIED_EMPTY', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  if (!sheetRows.length) {\n    return { ...base, state: 'WORKFLOW_NODE_FAILED', reason: 'ZERO_ROWS_WITHOUT_VERIFIED_EMPTY', coverageState, shouldWrite: false, sheetRows: [] };\n  }\n  return {\n    ...base,\n    state: workflowState,\n    reason: workflowState === 'CAPPED' ? 'BOUNDED_SHORTLIST_WRITTEN_WITH_CAP_DISCLOSED' : 'VERIFIED_SHORTLIST_READY',\n    coverageState,\n    shouldWrite: true,\n    sheetRows,\n  };\n}\n\nfunction renderBookingReceipt(result = {}, writtenRows = null) {\n  const labels = {\n    READY_TO_WRITE: 'Booking.com shortlist verified',\n    CAPPED: 'Booking.com bounded shortlist verified',\n    EMPTY_VERIFIED: 'Booking.com search verified empty',\n    PARTIAL: 'Booking.com search partial — no Sheet write',\n    FAILED: 'Booking.com source failed — no Sheet write',\n    ACTOR_FAILED_OR_ABORTED: 'Booking Hotels Actor failed or stopped',\n    WORKFLOW_NODE_FAILED: 'Workflow could not verify Booking.com output',\n  };\n  return {\n    state: result.state,\n    label: labels[result.state] ?? 'Booking.com shortlist result',\n    reason: result.reason ?? null,\n    rowsPrepared: Array.isArray(result.sheetRows) ? result.sheetRows.length : 0,\n    rowsWritten: Number.isInteger(writtenRows) ? writtenRows : null,\n    exactActorChargeUsd: Number.isFinite(Number(result.buyerChargeUsd)) ? Number(result.buyerChargeUsd) : null,\n    coverageState: result.coverageState ?? null,\n    baselineState: result.baselineState ?? 'NOT_APPLICABLE_STATELESS_SNAPSHOT',\n    unchangedState: result.unchangedState ?? 'NOT_CLAIMED_GOOGLE_SHEETS_UPSERT_ONLY',\n    runId: result.runId ?? null,\n    runUrl: result.runUrl ?? 'https://console.apify.com/actors/runs',\n    datasetUrl: result.datasetUrl ?? result.runUrl ?? 'https://console.apify.com/actors/runs',\n  };\n}\n\nconst BOOKING_HOTELS_WORKFLOW_LIMITS = Object.freeze({ maxPropertyRows: MAX_PROPERTY_ROWS, maxDatasetRows: MAX_DATASET_ROWS, pageSize: PAGE_SIZE });\n\n\nreturn [{ json: renderBookingReceipt($input.first().json) }];"
      },
      "id": "0dee4b813bbcd44592b63133",
      "name": "Return explicit no-write receipt",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        4040,
        240
      ]
    }
  ],
  "pinData": {},
  "connections": {
    "Test manually": {
      "main": [
        [
          {
            "node": "Shortlist settings",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Every day at 08:00 (inactive)": {
      "main": [
        [
          {
            "node": "Shortlist settings",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Shortlist settings": {
      "main": [
        [
          {
            "node": "Fail-closed preflight",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fail-closed preflight": {
      "main": [
        [
          {
            "node": "Run exact Booking Hotels Actor once",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Run exact Booking Hotels Actor once": {
      "main": [
        [
          {
            "node": "Classify terminal status",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Classify terminal status": {
      "main": [
        [
          {
            "node": "Run succeeded with storage IDs?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Run succeeded with storage IDs?": {
      "main": [
        [
          {
            "node": "Get documented OUTPUT summary",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Return explicit no-write receipt",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get documented OUTPUT summary": {
      "main": [
        [
          {
            "node": "Normalize authoritative summary",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize authoritative summary": {
      "main": [
        [
          {
            "node": "Summary valid?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Summary valid?": {
      "main": [
        [
          {
            "node": "Plan complete dataset pages",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Return explicit no-write receipt",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Plan complete dataset pages": {
      "main": [
        [
          {
            "node": "Get every property and coverage page",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get every property and coverage page": {
      "main": [
        [
          {
            "node": "Reconcile rows coverage and exact charge",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Reconcile rows coverage and exact charge": {
      "main": [
        [
          {
            "node": "Verified rows ready for upsert?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Verified rows ready for upsert?": {
      "main": [
        [
          {
            "node": "Expand stable upsert rows",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Return explicit no-write receipt",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Expand stable upsert rows": {
      "main": [
        [
          {
            "node": "Upsert dated shortlist in Google Sheets",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Upsert dated shortlist in Google Sheets": {
      "main": [
        [
          {
            "node": "Return successful Sheet receipt",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "timezone": "Europe/Podgorica"
  },
  "versionId": "82885e27-48be-56d7-ba82-e891403aaa9e",
  "id": "bookingHotelsSheetsV1",
  "tags": []
}
